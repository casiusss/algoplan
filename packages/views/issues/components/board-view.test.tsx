import { describe, it } from "vitest";

// Wave 0 RED scaffold — assertions land in Plan 01 (dnd-kit migration). Do NOT
// delete this file before Plan 01 merges; subsequent waves' <verify> commands
// reference it.
describe.skip("BoardView (Plan 01 dnd-kit, Plan 02 nothing, Plan 04 nothing)", () => {
  it("placeholder", () => {});
});
