import { describe, it } from "vitest";

// Wave 0 RED scaffold — assertions land in Plans 01/02/04. Do NOT delete this
// file before those plans merge; subsequent waves' <verify> commands reference
// it.
describe.skip("BoardColumn (Plan 01 dnd-kit, Plan 02 visuals, Plan 04 inline-add wiring)", () => {
  it("placeholder", () => {});
});
