import { describe, it } from "vitest";

// Wave 0 RED scaffold — assertions land in Plans 01/02. Do NOT delete this
// file before those plans merge; subsequent waves' <verify> commands reference
// it.
describe.skip("BoardCard (Plan 01 dnd-kit, Plan 02 AccentBar)", () => {
  it("placeholder", () => {});
});
