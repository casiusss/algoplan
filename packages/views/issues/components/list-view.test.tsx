import { describe, it } from "vitest";

// Wave 0 RED scaffold — assertions land in Plans 03/04. Do NOT delete this
// file before those plans merge; subsequent waves' <verify> commands reference
// it.
describe.skip("ListView (Plan 03 visuals, Plan 04 inline-add wiring)", () => {
  it("placeholder", () => {});
});
