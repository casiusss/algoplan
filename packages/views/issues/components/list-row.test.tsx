import { describe, it } from "vitest";

// Wave 0 RED scaffold — assertions land in Plan 03 (visuals). Do NOT delete
// this file before Plan 03 merges; subsequent waves' <verify> commands
// reference it.
describe.skip("ListRow (Plan 03 visuals)", () => {
  it("placeholder", () => {});
});
