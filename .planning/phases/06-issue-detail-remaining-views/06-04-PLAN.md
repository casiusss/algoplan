---
phase: 06-issue-detail-remaining-views
plan: 04
type: execute
wave: 1
depends_on: [06-00]
files_modified:
  - packages/views/agents/components/agents-page.tsx
  - packages/views/agents/components/agent-list-item.tsx
  - packages/views/agents/components/agent-detail.tsx
  - packages/views/agents/components/create-agent-dialog.tsx
  - packages/views/workspace/no-access-page.tsx
  - packages/views/workspace/no-access-page.test.tsx
  - packages/views/dashboard-shell/workspace-switcher.tsx
  - apps/web/app/not-found.tsx
autonomous: true
requirements: [WS-01, WS-02, WS-03, WS-04, WS-05]
must_haves:
  truths:
    - "AgentsPage renders German strings; empty states use <EmptyState> from Wave 0"
    - "AgentListItem uses <AvatarInitial> when agent has no image URL; existing image rendering preserved otherwise"
    - "NoAccessPage renders German strings + <AlgoPlanWordmark size='lg'/> above heading; DragStrip stays first flex child"
    - "Workspace switcher dropdown uses German strings: Workspace erstellen, Abmelden"
    - "apps/web/app/not-found.tsx exists and wraps <NotFoundPage> from @multica/views/common"
    - "WS-05 destructive-ops safe-order is UNCHANGED (no logic touched in workspace-tab.tsx — Plan 03 owns it; this plan does not regress)"
  artifacts:
    - path: "packages/views/agents/components/agents-page.tsx"
      provides: "Agents page with German strings + EmptyState empty branches"
      contains: "EmptyState"
    - path: "packages/views/workspace/no-access-page.tsx"
      provides: "No-access page with AlgoPlanWordmark + German strings"
      contains: "AlgoPlanWordmark"
    - path: "apps/web/app/not-found.tsx"
      provides: "Next.js 404 route mounting <NotFoundPage>"
      contains: "NotFoundPage"
  key_links:
    - from: "packages/views/agents/components/agents-page.tsx"
      to: "packages/views/workspace/empty-state.tsx (Wave 0)"
      via: "<EmptyState heading='Noch keine Agenten' /> for empty/active-hidden/archived branches"
      pattern: "EmptyState"
    - from: "packages/views/agents/components/agent-list-item.tsx"
      to: "@multica/ui/components/ui/avatar-initial"
      via: "<AvatarInitial name={agent.name}> when no image URL"
      pattern: "AvatarInitial"
    - from: "apps/web/app/not-found.tsx"
      to: "@multica/views/common/not-found-page (Wave 0)"
      via: "default export wrapping <NotFoundPage />"
      pattern: "NotFoundPage"
---

<objective>
Restyle Agents + NoAccessPage + workspace switcher with German strings + Wave-0 atoms (EmptyState, AvatarInitial, AlgoPlanWordmark). Wire the Next.js 404 route to mount the shared `<NotFoundPage>`.

WS-05 (destructive-ops safe order) is OWNED by Plan 03 (workspace-tab.tsx). This plan ONLY verifies via the existing test that nothing regressed. Desktop's silent-heal for stale workspace tabs is already implemented per UI-SPEC §Sub-Phase WS §WS-04 — no code changes needed here.

Purpose: Close out the WS sub-phase. EmptyState normalizes empty-state visuals across agents/inbox/board/list. AvatarInitial fallback removes the inconsistent gray-square no-avatar render. NoAccessPage gets the auth-page brand chrome.
Output: 4 RESTYLE + 1 EDIT (workspace-switcher) + 1 NEW (web 404 route) = ~6 file touches; 1 test edit (no-access-page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-issue-detail-remaining-views/06-CONTEXT.md
@.planning/phases/06-issue-detail-remaining-views/06-UI-SPEC.md
@CLAUDE.md
@packages/views/agents/components/agents-page.tsx
@packages/views/agents/components/agent-list-item.tsx
@packages/views/agents/components/agent-detail.tsx
@packages/views/agents/components/create-agent-dialog.tsx
@packages/views/workspace/no-access-page.tsx
@packages/views/workspace/no-access-page.test.tsx
@packages/views/dashboard-shell/workspace-switcher.tsx
@packages/views/workspace/empty-state.tsx
@packages/views/auth/algoplan-wordmark.tsx
@packages/views/common/not-found-page.tsx
@apps/web/app/layout.tsx

<interfaces>
<!-- From Wave 0: -->
<EmptyState illustration?={ReactNode} heading={string} body?={string} cta?={{label, onClick}} />
<AlgoPlanWordmark size?="default"|"lg" className?={string} />
<NotFoundPage />  // standalone full-window page; mounts DragStrip first

<!-- From @multica/ui (Phase 2): -->
<AvatarInitial name={string} size?="sm"|"md"|"lg" />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentsPage + AgentListItem + AgentDetail + CreateAgentDialog — German strings + EmptyState + AvatarInitial fallback</name>
  <files>
    packages/views/agents/components/agents-page.tsx,
    packages/views/agents/components/agent-list-item.tsx,
    packages/views/agents/components/agent-detail.tsx,
    packages/views/agents/components/create-agent-dialog.tsx
  </files>
  <behavior>
    Behavior to assert (no new dedicated test file; rely on integration via existing agents tests + manual verification — UI-SPEC §Sub-Phase WS does not enumerate explicit test files for these). If a `agents-page.test.tsx` already exists, update its string assertions. If not, this task does NOT add one.

    Manual + integration assertions:
    1. AgentsPage PageHeader title: `"Agenten"`.
    2. Create CTA: `"Agent erstellen"`.
    3. Empty (no agents) state: `<EmptyState heading="Noch keine Agenten" />` (Wave-0 component).
    4. Empty (active hidden, viewing only archived): `"Keine aktiven Agenten"`.
    5. Empty (archived branch with nothing): `"Keine archivierten Agenten"`.
    6. Detail-pane no-selection placeholder: `"Wähle einen Agenten."`.
    7. Archive show/hide tooltips: `"Archivierte Agenten anzeigen"` / `"Aktive Agenten anzeigen"`.
    8. AgentListItem: when `agent.image_url` is empty/null, renders `<AvatarInitial name={agent.name}>`. When present, renders `<img src={agent.image_url}>`.
    9. CreateAgentDialog and AgentDetail: German strings.
  </behavior>
  <action>
    1. **Read** all 4 files. Cache structure.

    2. **AgentsPage** — replace hardcoded empty-state JSX with `<EmptyState heading="..." />` from `@multica/views/workspace`. Three branches:
       ```tsx
       if (totalAgents === 0) return <EmptyState heading="Noch keine Agenten" />;
       if (showingArchived && archivedCount === 0) return <EmptyState heading="Keine archivierten Agenten" />;
       if (!showingArchived && activeCount === 0) return <EmptyState heading="Keine aktiven Agenten" />;
       ```
       PageHeader title → `"Agenten"`. Create CTA → `"Agent erstellen"`. Tooltips per UI-SPEC §Copywriting WS.

    3. **AgentListItem** — Read it first; identify the avatar rendering. Replace the no-image fallback path with `<AvatarInitial name={agent.name} size="sm" />`. Keep the `<img src={agent.image_url}>` path when image is present. Per UI-SPEC §Hard Constraints #12: "AvatarInitial wraps `<ActorAvatar>` no-image fallback — does NOT replace `<ActorAvatar>` everywhere. Image-bearing actors keep their image."

    4. **AgentDetail** — German strings only. Visual structure unchanged (per UI-SPEC §Sub-Phase WS §WS-02 table).

    5. **CreateAgentDialog** — German strings only.

    6. If existing test files (`agents-page.test.tsx` etc.) exist, update string assertions. If absent, no new test files added in this task (per UI-SPEC §Sub-Phase WS — does not require explicit test files for these RESTYLE rows; the dragstrip-coverage gate from Wave 0 + integration covers the surface).

    Per UI-SPEC §Hard Constraints #16: keep PageHeader pattern.
    Per UI-SPEC §Hard Constraints #8: no new tokens.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views exec vitest run agents/ &amp;&amp; pnpm --filter @multica/views exec tsc --noEmit</automated>
  </verify>
  <done>
    All German string changes compile + existing tests stay GREEN; AvatarInitial fallback wired in AgentListItem; EmptyState used in 3 empty branches; typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NoAccessPage RESTYLE + Workspace switcher German strings + Web 404 route</name>
  <files>
    packages/views/workspace/no-access-page.tsx,
    packages/views/workspace/no-access-page.test.tsx,
    packages/views/dashboard-shell/workspace-switcher.tsx,
    apps/web/app/not-found.tsx
  </files>
  <behavior>
    no-access-page.test.tsx EDITS — Tests:
    1. Title renders `"Workspace nicht verfügbar"`.
    2. Body renders `"Dieser Workspace existiert nicht oder du hast keinen Zugriff."`.
    3. Primary CTA: `"Zu meinen Workspaces"` — clicking navigates to /workspaces (or whatever the existing target is).
    4. Secondary CTA: `"Mit anderem Konto anmelden"`.
    5. `<AlgoPlanWordmark size="lg" />` is present above the heading.
    6. `<DragStrip />` is the first flex child of the page-root container (covered by Wave 0 dragstrip-coverage gate, but assert the wordmark is INSIDE the centered card region — NOT above DragStrip per UI-SPEC §AUTH §"Pitfall reminder").

    Workspace switcher (existing, lightly edited) — Tests (if existing tests exist; otherwise smoke-only):
    1. Dropdown header reads `"AlgoPlan"` (English brand — kept).
    2. Add-workspace item: `"Workspace erstellen"`.
    3. Sign-out item: `"Abmelden"`.

    apps/web/app/not-found.tsx — Tests (Next.js convention; no separate vitest, just typecheck + Next-build):
    1. Default export is a React component that renders `<NotFoundPage>` from `@multica/views/common/not-found-page`.
    2. The Web wrapper does NOT need its own DragStrip — `<NotFoundPage>` already mounts one as the first flex child of its own page root.
  </behavior>
  <action>
    1. **NoAccessPage** — Read the file. Identify the existing layout (DragStrip + centered region per UI-SPEC §DragStrip Enumeration table).
       - Insert `<AlgoPlanWordmark size="lg" className="mb-6" />` INSIDE the centered region, ABOVE the heading. **Critical**: DragStrip stays the FIRST flex child of the root flex container (do not push it down).
       - Update strings to German per UI-SPEC §Copywriting WS:
         - Title: `"Workspace nicht verfügbar"`
         - Body: `"Dieser Workspace existiert nicht oder du hast keinen Zugriff."`
         - Primary CTA: `"Zu meinen Workspaces"`
         - Secondary CTA: `"Mit anderem Konto anmelden"`

    2. **no-access-page.test.tsx** — update string assertions; add the wordmark-presence assertion. Verify the dragstrip-coverage gate still passes (Wave-0 test).

    3. **Workspace switcher** — Read `packages/views/dashboard-shell/workspace-switcher.tsx`. Identify dropdown items:
       - Add-workspace item label → `"Workspace erstellen"`.
       - Sign-out item label → `"Abmelden"`.
       - Header `"AlgoPlan"` stays English (brand).
       Visual unchanged per UI-SPEC §Sub-Phase WS §WS-01.

    4. **Web 404 route** — `apps/web/app/not-found.tsx`:
       ```tsx
       import { NotFoundPage } from "@multica/views/common/not-found-page";

       export default function NotFound() {
         return <NotFoundPage />;
       }
       ```
       Per Next.js convention: `app/not-found.tsx` is the default 404 handler. The shared `<NotFoundPage>` already includes DragStrip + AlgoPlanWordmark + brand-green CTA per Wave 0.
       **CLAUDE.md alignment**: this file lives in `apps/web/app/` — the only place Next.js APIs may be referenced. The wrapper just imports the shared view. ✓

    5. **Verify dragstrip-coverage gate** is still GREEN — `<NotFoundPage>` was added to the gate's enumeration in Wave 0; the NoAccessPage edit must not break it.

    Per UI-SPEC §Hard Constraints #1: DragStrip MUST be the FIRST flex child. Wordmark goes INSIDE the centered card region.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views exec vitest run workspace/no-access-page.test.tsx __tests__/dragstrip-coverage.test.ts &amp;&amp; pnpm --filter @multica/web exec tsc --noEmit &amp;&amp; pnpm --filter @multica/views exec tsc --noEmit</automated>
  </verify>
  <done>
    no-access-page.test.tsx GREEN (German strings + wordmark); dragstrip-coverage gate GREEN (no regressions); workspace-switcher German strings compile; apps/web/app/not-found.tsx wraps NotFoundPage; all typechecks clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user navigation → 404 | Unknown URL falls through to NotFoundPage; CTA navigates to root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W1-WS-01 | Information Disclosure | NoAccessPage | accept | Already constant copy regardless of whether the workspace exists or the user lacks access — does NOT distinguish "not found" from "no access". Mirrors Phase 5.1's no-enumeration pattern. |
| T-06-W1-WS-02 | Spoofing | apps/web/app/not-found.tsx | accept | Next.js's built-in 404 mechanism; CTA navigates to internal `/` only. |
| T-06-W1-WS-03 | Tampering | DragStrip placement | mitigate | Wave-0 dragstrip-coverage gate enforces first-flex-child constraint on every full-window view; this plan's NoAccessPage edit is verified by the gate. |
</threat_model>

<verification>
```bash
pnpm --filter @multica/views exec vitest run agents workspace/no-access-page.test.tsx __tests__/dragstrip-coverage.test.ts
pnpm --filter @multica/views exec tsc --noEmit
pnpm --filter @multica/web exec tsc --noEmit
```
All Vitest GREEN. Typechecks clean.
</verification>

<success_criteria>
- WS-01 (workspace switcher): German dropdown items (Workspace erstellen, Abmelden); brand "AlgoPlan" header kept
- WS-02 (agents): German strings; AvatarInitial fallback for image-less agents; AgentDetail + CreateAgentDialog German
- WS-03 (empty states): AgentsPage uses Wave-0 `<EmptyState>` for 3 empty branches
- WS-04 (error states): apps/web/app/not-found.tsx wraps `<NotFoundPage>`; NoAccessPage gets German strings + AlgoPlanWordmark; desktop silent-heal unchanged (no code change needed)
- WS-05 (safe order): owned by Plan 03; this plan verifies the existing test stays GREEN
- DragStrip gate stays GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/06-issue-detail-remaining-views/06-04-SUMMARY.md` covering:
- Agents avatar fallback decision (AvatarInitial wraps no-image branch only)
- Web 404 wiring (Next.js `app/not-found.tsx` convention)
- Confirmation that DragStrip gate stayed GREEN throughout
- Note that desktop silent-heal for stale workspace tabs needs no code change
</output>
