---
phase: 06-issue-detail-remaining-views
plan: 03
type: execute
wave: 1
depends_on: [06-00]
files_modified:
  - packages/views/settings/components/settings-page.tsx
  - packages/views/settings/components/settings-page.test.tsx
  - packages/views/settings/components/appearance-tab.tsx
  - packages/views/settings/components/appearance-tab.test.tsx
  - packages/views/settings/components/workspace-tab.tsx
  - packages/views/settings/components/workspace-tab.test.tsx
  - packages/views/settings/components/account-tab.tsx
  - packages/views/settings/components/members-tab.tsx
  - packages/views/settings/components/repositories-tab.tsx
  - packages/views/settings/components/tokens-tab.tsx
  - packages/views/settings/components/delete-workspace-dialog.tsx
  - packages/views/settings/components/delete-workspace-dialog.test.tsx
autonomous: true
requirements: [SET-01, SET-02, SET-03]
must_haves:
  truths:
    - "Settings left-nav uses bg-sidebar tone (replaces bg-background)"
    - "Settings page text is German per UI-SPEC §Copywriting Contract SET (Mein Konto, Workspace, Profil, Erscheinungsbild, API-Tokens, Allgemein, Repositories, Mitglieder)"
    - "Workspace group has a [Gefahrenzone] quick-jump button that scrolls to the danger-zone section"
    - "Each settings tab body is wrapped in <SettingsSection> (default tone) — Workspace tab Danger Zone uses tone='danger'"
    - "AppearanceTab renders 3 mockup radio options (Hell / Dunkel / System); active option ring-2 ring-brand + brand-green check-icon at top-right corner of mockup"
    - "AppearanceTab calls setTheme via useTheme() (storageKey='multica_theme')"
    - "Phase 4 sidebar DarkModeToggle and AppearanceTab share state — toggling one reflects in the other"
    - "DeleteWorkspaceDialog typed-name confirmation gate preserved (German strings only)"
    - "WorkspaceTab navigateAwayFromCurrentWorkspace safe-order is UNCHANGED (read dest → setCurrentWorkspace(null,null) → push → mutate)"
  artifacts:
    - path: "packages/views/settings/components/settings-page.tsx"
      provides: "Tabs orientation=vertical with German left-nav strings + Gefahrenzone quick-jump"
      contains: "Gefahrenzone"
    - path: "packages/views/settings/components/workspace-tab.tsx"
      provides: "Workspace settings wrapped in <SettingsSection>; Danger Zone uses tone='danger'"
      contains: "SettingsSection"
  key_links:
    - from: "packages/views/settings/components/settings-page.tsx"
      to: "Gefahrenzone scroll target"
      via: "<a href='#danger-zone' onClick={scrollIntoView}>"
      pattern: "danger-zone|Gefahrenzone"
    - from: "packages/views/settings/components/appearance-tab.tsx"
      to: "@multica/ui/components/common/theme-provider useTheme()"
      via: "setTheme('light'|'dark'|'system')"
      pattern: "useTheme|setTheme"
    - from: "packages/views/settings/components/workspace-tab.tsx"
      to: "navigateAwayFromCurrentWorkspace"
      via: "existing safe-order helper — UNCHANGED"
      pattern: "navigateAwayFromCurrentWorkspace"
---

<objective>
Restructure Settings: sectioned layout via `<SettingsSection>` from Wave 0 (SET-01), German strings throughout (SET-01 + SET-02 + SET-03), brand-green check-icon overlay on the active dark-mode mockup (SET-02 enhancement), and Danger Zone visual signal via `tone="danger"` (SET-03 visual). Logic for `navigateAwayFromCurrentWorkspace` and `DeleteWorkspaceDialog` typed-name gating is UNCHANGED.

Per UI-SPEC §Sub-Phase SET §Sectioned layout: existing `Tabs orientation="vertical"` is KEPT. Only the section wrapping + strings + Danger Zone quick-jump are NEW.

Purpose: Settings is the highest-friction admin surface. SettingsSection wrapper unifies the visual rhythm; Danger Zone's destructive ring + heading dot signals "be careful here" without being noisy; the dark-mode radio finally gets a check-icon affordance to reinforce the active state.
Output: 1 RESTYLE + 6 RESTYLE/COPY tabs + 1 LIGHT EDIT dialog = ~10 file touches; ~2-3 test files edited (the others are string-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-issue-detail-remaining-views/06-CONTEXT.md
@.planning/phases/06-issue-detail-remaining-views/06-UI-SPEC.md
@CLAUDE.md
@packages/views/settings/components/settings-page.tsx
@packages/views/settings/components/appearance-tab.tsx
@packages/views/settings/components/workspace-tab.tsx
@packages/views/settings/components/workspace-tab.test.tsx
@packages/views/settings/components/account-tab.tsx
@packages/views/settings/components/members-tab.tsx
@packages/views/settings/components/repositories-tab.tsx
@packages/views/settings/components/tokens-tab.tsx
@packages/views/settings/components/delete-workspace-dialog.tsx
@packages/views/settings/components/delete-workspace-dialog.test.tsx
@packages/views/settings/index.ts
@packages/views/settings/components/settings-section.tsx
@packages/ui/components/common/theme-provider

<interfaces>
<!-- From Wave 0 (06-00 plan): -->
<SettingsSection
  heading: string
  tone?: "default" | "danger"
  id?: string
  children: React.ReactNode
/>

<!-- From @multica/ui/components/common/theme-provider (Phase 1 — already exists): -->
useTheme(): { theme: "light" | "dark" | "system" | undefined; setTheme: (t) => void; resolvedTheme: "light" | "dark" }
// storageKey="multica_theme" (already configured per Phase 1)

<!-- From existing workspace-tab.tsx (DO NOT change this logic — only strings + section wrapper): -->
navigateAwayFromCurrentWorkspace(destinationSlug: string): void
// Safe order: 1) read destination, 2) setCurrentWorkspace(null,null), 3) navigation.push(destination), 4) THEN mutation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsPage left-nav German strings + bg-sidebar tone + Gefahrenzone quick-jump</name>
  <files>
    packages/views/settings/components/settings-page.tsx,
    packages/views/settings/components/settings-page.test.tsx
  </files>
  <behavior>
    settings-page.test.tsx — Tests:
    1. Left nav renders German group headings: `"Mein Konto"` and `"Workspace"`.
    2. Tab labels render in German: `Profil`, `Erscheinungsbild`, `API-Tokens`, `Allgemein`, `Repositories`, `Mitglieder`.
    3. Page title (sidebar header) renders `"Einstellungen"`.
    4. Left nav has `bg-sidebar` class on its container (replaces `bg-background`).
    5. The Workspace group includes a `[Gefahrenzone]` quick-jump button.
    6. Clicking `[Gefahrenzone]` navigates to the Workspace tab (if not already there) AND scrolls to `#danger-zone` element. Test asserts: a click triggers `scrollIntoView` (mocked) on the section with id `danger-zone`. (Exact mechanism: mock `Element.prototype.scrollIntoView`; or assert `location.hash === '#danger-zone'` after click; pick whichever matches the existing pattern in the project.)
  </behavior>
  <action>
    1. Read existing `settings-page.tsx`. Identify:
       - The `Tabs orientation="vertical"` block.
       - The left-nav container (currently `bg-background`).
       - The group label rendering for "My Account" + "Workspace".
       - The tab list rendering.

    2. **String updates** — replace English with German per UI-SPEC §Copywriting Contract SET:
       - Page title: `"Einstellungen"`
       - Group: `"Mein Konto"` + `"Workspace"` (or `{workspace.name}` if existing fallback)
       - Tabs: `Profil` / `Erscheinungsbild` / `API-Tokens` / `Allgemein` / `Repositories` / `Mitglieder`
       - Replace `bg-background` on the left-nav container with `bg-sidebar` (per UI-SPEC §Sub-Phase SET §Sectioned layout §Outer container).

    3. **Gefahrenzone quick-jump** — Inside the Workspace group label area, add a small affordance:
       ```tsx
       <button
         type="button"
         className="text-xs text-muted-foreground hover:text-destructive"
         onClick={() => {
           // Switch to workspace tab, then scroll to danger-zone after the next tick.
           setActiveTab("workspace");
           requestAnimationFrame(() => {
             document.getElementById("danger-zone")?.scrollIntoView({ behavior: "smooth", block: "start" });
           });
         }}
       >
         Gefahrenzone
       </button>
       ```
       (Adapt to the existing tabs API in the file — if the page uses Base UI `Tabs` with `value`+`onValueChange`, use that.)

    4. **Test edits** — update existing settings-page.test.tsx string assertions; add the new `Gefahrenzone` quick-jump test using `vi.spyOn(HTMLElement.prototype, "scrollIntoView")`.

    Per UI-SPEC §Hard Constraints #16: keep the existing PageHeader pattern (do NOT replace with AppTopbar).
    Per CLAUDE.md `state-management`: no new Zustand stores; the active-tab state already exists.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views exec vitest run settings/components/settings-page.test.tsx</automated>
  </verify>
  <done>
    All German label assertions GREEN; bg-sidebar class on left-nav container; Gefahrenzone quick-jump test GREEN; typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppearanceTab — German labels + brand-green check-icon overlay + sync-with-sidebar test</name>
  <files>
    packages/views/settings/components/appearance-tab.tsx,
    packages/views/settings/components/appearance-tab.test.tsx
  </files>
  <behavior>
    appearance-tab.test.tsx — Tests:
    1. Renders 3 radio options labeled `Hell`, `Dunkel`, `System` (German per Copywriting SET).
    2. Section heading renders `"Theme"` (German same word per Copywriting).
    3. Each radio's aria-label: `"Hell auswählen"` / `"Dunkel auswählen"` / `"System auswählen"`.
    4. Group root aria-label: `"Theme auswählen"`.
    5. Clicking each option calls `setTheme` with `"light"` / `"dark"` / `"system"` respectively.
    6. Active option's mockup has `ring-2 ring-brand` class.
    7. Active option's mockup ALSO has a brand-green check-icon at top-right corner (`absolute top-1 right-1 text-brand`).
    8. Sync-with-sidebar test: simulate the `useTheme` hook returning a different theme value (e.g. via mocked module re-render) → assert the active radio reflects the new value.
  </behavior>
  <action>
    1. Read existing `appearance-tab.tsx`. Identify:
       - The 3-option radio rendering (likely `<button role="radio">` per existing convention).
       - The `useTheme()` hook usage.
       - The `ring-2 ring-brand` active-state styling.

    2. **String updates** — German labels:
       - Section heading: `"Theme"`
       - Option labels: `Hell` / `Dunkel` / `System`
       - aria-labels per UI-SPEC §Copywriting Contract SET §AppearanceTab radio aria-label.

    3. **Check-icon overlay** — wrap each mockup with a position-relative container; conditionally render the icon when active:
       ```tsx
       {isActive && (
         <span className="absolute top-1 right-1">
           <Check className="size-4 text-brand" aria-hidden />
         </span>
       )}
       ```
       Per UI-SPEC §Color Accent reserved-for §11: the brand-green check-mark at top-right of the active mockup.

    4. **useTheme** — verify the hook is imported from `@multica/ui/components/common/theme-provider` (per UI-SPEC §Sub-Phase SET §Dark-mode radio). If not (e.g. directly from `next-themes`), update the import. The hook MUST use `storageKey="multica_theme"` — Phase 1 already configured this; verify and don't fork.

    5. **Test edits** — update string assertions; add the check-icon presence assertion (assert via lucide icon class or `data-testid`); add the click-each-option-fires-setTheme test (mock `useTheme`); add the sync test (re-render with different theme value, assert the matching radio is now active).

    6. Existing `WindowMockup` `dark:*` literals are GRANDFATHERED per UI-SPEC §Color §Dark mode — DO NOT remove. They're preview-only.

    Per UI-SPEC §Hard Constraints #4: settings dark-mode radio MUST persist via `multica_theme` localStorage. The Phase 1 wrapper handles this — verify and don't double-write.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views exec vitest run settings/components/appearance-tab.test.tsx</automated>
  </verify>
  <done>
    All German label assertions GREEN; check-icon overlay renders only on active mockup; setTheme called with correct value per option; sync-with-sidebar test GREEN; typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WorkspaceTab + AccountTab + MembersTab + RepositoriesTab + TokensTab + DeleteWorkspaceDialog — section wrappers + German strings + tone='danger'</name>
  <files>
    packages/views/settings/components/workspace-tab.tsx,
    packages/views/settings/components/workspace-tab.test.tsx,
    packages/views/settings/components/account-tab.tsx,
    packages/views/settings/components/members-tab.tsx,
    packages/views/settings/components/repositories-tab.tsx,
    packages/views/settings/components/tokens-tab.tsx,
    packages/views/settings/components/delete-workspace-dialog.tsx,
    packages/views/settings/components/delete-workspace-dialog.test.tsx
  </files>
  <behavior>
    workspace-tab.test.tsx EDITS — Tests:
    1. Section headings render German: `"Allgemein"`, `"Gefahrenzone"`, `"Workspace verlassen"`, `"Workspace löschen"`.
    2. Field labels render German: `Name`, `Beschreibung`, `Kontext`, `Slug`.
    3. Save button text: `"Speichern"`; in-progress: `"Wird gespeichert…"`.
    4. Non-admin hint: `"Nur Admins und Owner können Workspace-Einstellungen ändern."`.
    5. Each section is wrapped in `<SettingsSection>`; the Danger Zone is `<SettingsSection tone="danger" id="danger-zone">` (the `id` is the scroll target for the quick-jump from Task 1).
    6. **CRITICAL — preserve existing safe-order test verbatim**: `navigateAwayFromCurrentWorkspace(destinationSlug)` is called in this exact order: read destination → setCurrentWorkspace(null,null) → navigation.push(destination) → THEN mutation.mutateAsync. This existing test MUST continue passing without changes.
    7. Leave button: `"Workspace verlassen"`; in-progress: `"Wird verlassen…"`.

    delete-workspace-dialog.test.tsx EDITS — Tests:
    1. Dialog title: `"Workspace löschen"`.
    2. Dialog body: `"Diese Aktion kann nicht rückgängig gemacht werden. Alle Issues, Agenten und Daten werden dauerhaft entfernt."`.
    3. Confirm-instruction: `"Zur Bestätigung tippe \`{workspaceName}\` unten ein."` (German with backtick markup preserved).
    4. Cancel: `"Abbrechen"`; confirm button: `"Workspace löschen"`; in-progress: `"Wird gelöscht…"`.
    5. **CRITICAL — typed-name gating preserved**: existing test that asserts the confirm button is disabled until typed name matches `workspace.name` MUST continue passing.

    AccountTab / MembersTab / RepositoriesTab / TokensTab — section wrappers + German strings (no separate test files added per UI-SPEC FLAG-2.2 note; covered by `settings-page.test.tsx` integration). For files without existing tests, add a minimal smoke test asserting the section wrapper + 1-2 German labels render.
  </behavior>
  <action>
    1. **Read** all 7 files at the start (single Read per file). Cache structure.

    2. **WorkspaceTab** — `workspace-tab.tsx`:
       - Wrap each existing section in `<SettingsSection heading="...">`. Specifically:
         - General settings → `<SettingsSection heading="Allgemein">`
         - Danger Zone (Leave + Delete buttons) → `<SettingsSection heading="Gefahrenzone" tone="danger" id="danger-zone">`
       - Update strings to German per Copywriting SET.
       - **DO NOT touch `navigateAwayFromCurrentWorkspace()` logic** — per UI-SPEC §Hard Constraints #11 + WS-05.
       - **DO NOT touch the Leave/Delete order** — per CLAUDE.md `Workspace destructive operations` (read dest → setCurrentWorkspace(null,null) → push → mutate).
       - Run existing `workspace-tab.test.tsx` — the safe-order test MUST stay GREEN as-is. Update only the string assertions.

    3. **AccountTab** — wrap existing content in `<SettingsSection heading="Profil">` (or whichever heading matches); German strings; AvatarInitial fallback for the avatar (replace any inline no-image render with `<AvatarInitial name={user.name} />`).

    4. **MembersTab** — wrap in `<SettingsSection heading="Mitglieder">`; German strings; AvatarInitial for member rows without avatar URL.

    5. **RepositoriesTab** — wrap in `<SettingsSection heading="Repositories">`; German strings.

    6. **TokensTab** — wrap in `<SettingsSection heading="API-Tokens">`; German strings.

    7. **DeleteWorkspaceDialog** — German strings only (per UI-SPEC §Sub-Phase SET §Danger Zone: "Phase 6 changes: German strings only — visual unchanged"). The `<code>{workspaceName}</code>` element stays `font-mono` (existing — UI-SPEC §Typography §Font-family token notes this exception).

    8. **Test edits** — update string assertions in workspace-tab.test.tsx and delete-workspace-dialog.test.tsx. PRESERVE the safe-order test + typed-name gating test verbatim.

    9. **Smoke tests** — for AccountTab / MembersTab / RepositoriesTab / TokensTab, if no `.test.tsx` exists, add a minimal one:
       ```tsx
       describe("FooTab", () => {
         it("renders SettingsSection with German heading", () => {
           render(<FooTab {...mockProps} />);
           expect(screen.getByText("Foo-Heading-DE")).toBeInTheDocument();
         });
       });
       ```
       Per UI-SPEC §Sub-Phase SET note resolving FLAG-2.2: smoke tests are at planner discretion for these RESTYLE+COPY rows. **Decision: add smoke tests** (~5 min each) so the section wrapper presence + German heading are guarded against future regression without relying on the broader integration test.

    Per CLAUDE.md `Coding Rules` "Unless the user explicitly asks for backwards compatibility, do not add compatibility layers" — do NOT keep parallel English-string code paths. Replace inline.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views exec vitest run settings/components/workspace-tab.test.tsx settings/components/delete-workspace-dialog.test.tsx settings/components/account-tab.test.tsx settings/components/members-tab.test.tsx settings/components/repositories-tab.test.tsx settings/components/tokens-tab.test.tsx</automated>
  </verify>
  <done>
    All settings tab tests GREEN; existing safe-order test stays GREEN unchanged; existing typed-name gating test stays GREEN unchanged; German strings everywhere; SettingsSection wrappers in place; danger-zone id is the scroll target; typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → workspace destructive op | Leave / Delete workspace mutations are destructive; safe-order pattern guards against renderer hard-reload during concurrent refetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W1-SET-01 | Tampering / accidental destruction | DeleteWorkspaceDialog | mitigate | Typed-name confirmation gate preserved (existing test guards). Confirm button disabled until typed name matches `workspace.name`. Per UI-SPEC §Hard Constraints #10. |
| T-06-W1-SET-02 | Denial of Service | navigateAwayFromCurrentWorkspace | mitigate | Safe-order pattern (read dest → setCurrentWorkspace(null,null) → push → mutate) prevents renderer hard-reload from concurrent refetch races. UI-SPEC §Hard Constraints #11; existing test guards. NOT changed in this plan. |
| T-06-W1-SET-03 | Information Disclosure | Settings tabs (member emails, repo URLs) | accept | Existing access controls (admin-only mutations) unchanged. Phase 6 only restyles + translates strings. |
</threat_model>

<verification>
```bash
pnpm --filter @multica/views exec vitest run settings
pnpm --filter @multica/views exec tsc --noEmit
# Sanity: confirm safe-order regression test still passes
pnpm --filter @multica/views exec vitest run settings/components/workspace-tab.test.tsx --reporter=verbose | grep -E "navigateAway|safe.order|safe order"
```
All Vitest GREEN. Typecheck clean. Safe-order test unchanged + still GREEN.
</verification>

<success_criteria>
- SET-01 (sectioned layout): every tab body wrapped in `<SettingsSection>`; left-nav has Gefahrenzone quick-jump that scrolls to `#danger-zone`
- SET-02 (dark-mode radio): German labels (Hell/Dunkel/System); brand-green check-icon overlay on active mockup; setTheme via useTheme() with `multica_theme` storageKey; sync-with-sidebar test GREEN
- SET-03 (Danger Zone): typed-name confirmation gate preserved (verbatim test); German strings; Danger Zone uses `<SettingsSection tone="danger">` for visual ring
- All German strings per Copywriting Contract SET
- Safe-order regression test (`navigateAwayFromCurrentWorkspace`) stays GREEN unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-issue-detail-remaining-views/06-03-SUMMARY.md` covering:
- Section wrapping decisions per tab (default vs danger tone)
- Smoke-test-add decision for AccountTab/MembersTab/RepositoriesTab/TokensTab (added or skipped + justification)
- Verification that safe-order regression test stayed GREEN unchanged
- Any English literals discovered + handled outside the Copywriting table
</output>
