---
phase: 08-internal-rebrand-completion
plan: 05
type: execute
wave: 3
depends_on: ["08-01"]
files_modified:
  - server/cmd/multica/cmd_agent.go
  - server/cmd/multica/cmd_auth.go
  - server/cmd/multica/cmd_login.go
  - server/cmd/multica/cmd_issue.go
  - server/cmd/multica/cmd_workspace.go
  - server/cmd/multica/cmd_autopilot.go
  - server/cmd/multica/cmd_repo.go
  - server/cmd/multica/cmd_daemon.go
  - server/cmd/multica/cmd_agent_test.go
  - server/cmd/multica/cmd_auth_test.go
  - server/cmd/multica/main.go
  - server/cmd/multica/help.go
  - server/internal/cli/flags.go
  - server/internal/daemon/config.go
  - server/internal/handler/skill_test.go
  - docker-compose.selfhost.yml
  - docker-compose.selfhost.build.yml
  - turbo.json
  - Makefile
  - .env.example
autonomous: true
requirements: [RBR-09]
must_haves:
  truths:
    - "All Go call sites that previously read os.Getenv(\"MULTICA_X\") or cli.FlagOrEnv(..., \"MULTICA_X\", ...) now use the new ALGOPLAN_X name (via config.GetEnv or updated FlagOrEnv argument)"
    - "docker-compose.selfhost.yml ${MULTICA_X} variable references are renamed to ${ALGOPLAN_X} with the dual-read shim active in Go code"
    - "Makefile ALL caps variable names (MULTICA_APP_URL, MULTICA_SERVER_URL, MULTICA_ARGS) are renamed to ALGOPLAN_*"
    - ".env.example documents the new ALGOPLAN_* names with a deprecation header noting MULTICA_* still works for one release cycle"
    - "turbo.json globalEnv contains ALGOPLAN_SERVER_URL (replacing MULTICA_SERVER_URL)"
    - "Help text strings (help.go EnvVar references, daemon flag help) name ALGOPLAN_* not MULTICA_*"
    - "make test, make build, and `cd server && go test ./...` all exit 0"
  artifacts:
    - path: server/cmd/multica/cmd_agent.go
      provides: "All MULTICA_* env reads replaced with config.GetEnv(\"ALGOPLAN_*\")"
    - path: server/internal/cli/flags.go
      provides: "FlagOrEnv signature unchanged; callers pass ALGOPLAN_X env name; dual-read happens internally OR via config.GetEnv wrapper"
    - path: docker-compose.selfhost.yml
      provides: "Service env block uses ALGOPLAN_APP_URL; image refs still ${ALGOPLAN_BACKEND_IMAGE} / ${ALGOPLAN_WEB_IMAGE} (Plan 08-07 owns image NAME suffix; this plan only renames the env-VAR names)"
    - path: .env.example
      provides: "Documents ALGOPLAN_* names; legacy MULTICA_* listed in deprecated section with removal target"
  key_links:
    - from: server/cmd/multica/cmd_agent.go
      to: server/internal/config/env.go
      via: "imports config.GetEnv for dual-read"
      pattern: "internal/config"
    - from: docker-compose.selfhost.yml
      to: server/internal/config/env.go
      via: "Go reads ALGOPLAN_X from env; docker-compose passes it"
      pattern: "ALGOPLAN_APP_URL"
---

<objective>
Sweep every Go call site that reads `MULTICA_*` env vars and rewrite to read `ALGOPLAN_*` via the dual-read shim from Plan 08-01. Update docker-compose, Makefile, .env.example, and turbo.json env-name lists to match. Self-hosters with old `.env` files keep working (the shim emits a one-shot warning); new self-hosters see the new names.

Purpose: This closes the env-var half of D-3. After this plan, the codebase reads `ALGOPLAN_X` first; Plan 08-01's shim handles fallback. CONTEXT D-3 explicitly requires "deprecation warning" — that ships dormant in Plan 08-01 and activates here through the call-site rewrites.

Output: ~12 Go files updated with consistent `config.GetEnv("ALGOPLAN_X")` calls, docker-compose env names updated, Makefile renamed, .env.example regenerated with deprecated section, turbo.json env list updated. `cd server && go test ./...` green; existing test files (e.g. `cmd_agent_test.go`) updated to set `ALGOPLAN_*` env names.

This plan is Wave 3 — depends on 08-01 (the shim). Independent of 08-04 (TS files) and 08-06 (CLI binary rename file structure).

NOT IN SCOPE for this plan:
- Renaming `cmd/multica/` directory to `cmd/algoplan/` — Plan 08-06 owns
- Renaming `multica` binary to `algoplan` — Plan 08-06 owns
- Renaming docker IMAGE NAME suffix `multica-backend` → `algoplan-backend` — Plan 08-07 owns (release pipeline)
- Renaming `RESEND_FROM_EMAIL` default — Plan 08-07 owns

This plan focuses ONLY on env-var NAME flipping (Plan 08-01's consumer side), keeping the file structure under `server/cmd/multica/` until Plan 08-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-internal-rebrand-completion/08-CONTEXT.md
@.planning/phases/08-internal-rebrand-completion/08-01-SUMMARY.md
@CLAUDE.md
@server/internal/config/env.go
@server/cmd/multica/cmd_agent.go
@server/cmd/multica/cmd_auth.go
@server/cmd/multica/cmd_login.go
@server/cmd/multica/cmd_daemon.go
@server/cmd/multica/main.go
@server/cmd/multica/help.go
@server/internal/cli/flags.go
@server/internal/daemon/config.go
@docker-compose.selfhost.yml
@docker-compose.selfhost.build.yml
@.env.example
@Makefile
@turbo.json

<interfaces>
<!-- The shim (from Plan 08-01): -->
```go
package config // server/internal/config
func GetEnv(name string) string  // panics if name doesn't start with "ALGOPLAN_"
func GetEnvDefault(name, defaultValue string) string
```

<!-- Existing CLI helper (server/internal/cli/flags.go) — accepts an env-name argument: -->
```go
package cli
func FlagOrEnv(cmd *cobra.Command, flagName, envName, defaultValue string) string
```

<!-- The simplest consumer rewrite is mechanical: -->
```go
// Before
v := os.Getenv("MULTICA_X")
v := cli.FlagOrEnv(cmd, "flag", "MULTICA_X", "")
v := envOrDefault("MULTICA_X", "default")  // daemon helper

// After
import "github.com/multica-ai/multica/server/internal/config"
v := config.GetEnv("ALGOPLAN_X")
v := cli.FlagOrEnv(cmd, "flag", "ALGOPLAN_X", "")
v := envOrDefault("ALGOPLAN_X", "default")
```

<!-- For cli.FlagOrEnv: the helper internally calls os.Getenv. Plan 08-05 has TWO options: -->
<!-- Option A: change FlagOrEnv to call config.GetEnv internally — DRY, but changes ALL callers' behavior -->
<!-- Option B: leave FlagOrEnv as-is, ALSO call config.GetEnv as fallback at call sites -->
<!-- DECISION: Option A. Update FlagOrEnv to use config.GetEnv. Then call sites just pass "ALGOPLAN_X" as the env-name argument and get dual-read for free. This is the DRY/SOLID choice per CLAUDE.md. -->

<!-- Files to sweep (verified via grep audit 2026-04-27): -->

server/cmd/multica/ Go files containing MULTICA_*:
- cmd_agent.go (lines 165, 170, 173, 180, 203, 204, 210, 214, 235, 237) — many MULTICA_* reads
- cmd_auth.go (line 51, 60) — MULTICA_TOKEN, MULTICA_APP_URL
- cmd_login.go (line 18) — MULTICA_APP_URL
- cmd_issue.go (line 1104) — MULTICA_WORKSPACE_ID error message
- cmd_workspace.go (lines 92, 138) — MULTICA_WORKSPACE_ID error message
- cmd_autopilot.go (line 583) — MULTICA_WORKSPACE_ID error message
- cmd_repo.go (lines 35, 37, 40, 41, 42) — MULTICA_DAEMON_PORT, MULTICA_WORKSPACE_ID, MULTICA_AGENT_NAME, MULTICA_TASK_ID
- cmd_daemon.go (lines 62-68, 78-84, 94, 262, 296) — flag help strings + MULTICA_LAUNCHED_BY
- main.go (lines 35, 36) — flag help strings
- help.go (lines 128, 129) — env-var documentation block
- cmd_agent_test.go (lines 12, 13, 29-74) — t.Setenv calls
- cmd_auth_test.go (lines 21, 22, 31, 60, 101) — t.Setenv calls + URL strings

server/internal/cli/flags.go — needs FlagOrEnv to consume config.GetEnv
server/internal/daemon/config.go — envOrDefault calls (lines 71, 82, 86, 89, 93, 96, 100, 103, 107, 110, 114)
server/internal/handler/skill_test.go — MULTICA_RUN_SKILLS_SH_INTEGRATION (lines 367, 368) — preserved as opt-in test gate; rename to ALGOPLAN_RUN_SKILLS_SH_INTEGRATION

server/internal/handler/auth.go (line 154) — `r.Cookie("multica_signup_source")` — this is a COOKIE name, not an env var. Decision: keep as `multica_signup_source` for one release cycle? OR rename to `algoplan_signup_source` and lose tracking continuity? **Decision: rename to `algoplan_signup_source` — it's a server-set cookie, not user-controlled. No backwards-compat issue. Note in SUMMARY.**

server/internal/middleware/auth.go (lines 21, 101) — comments referencing `multica_auth` cookie. The cookie itself is renamed below.
server/internal/auth/cookie.go (lines 19, 20) — `AuthCookieName = "multica_auth"`, `CSRFCookieName = "multica_csrf"`. **DECISION: rename to `algoplan_auth` / `algoplan_csrf` — these are server-set HttpOnly cookies; on first request after upgrade, the server sets the new cookie name and the old one expires from disuse. There is a brief auth-loss window for users mid-session at deploy time, but this is acceptable for v0.5.0 since the same scope-rename was deferred in Phase 7 with the same understanding. Plan 08-08 verification gate notes this as a v0.5.0 release-comm item.**

Wait — re-reading carefully, these cookie names live in middleware that serves all requests. Renaming them means: on first request after deploy, the user has no `algoplan_auth` cookie set, so middleware/auth.go falls through to "unauthenticated" and the user is forced to re-login. This is a session-loss event for ALL active users, not just localStorage migration users.

**REVISED DECISION: cookies (`multica_auth`, `multica_csrf`, `multica_signup_source`) stay as `multica_*` for this plan**, with a TODO comment noting future migration. They are NOT MULTICA_* env vars (the prefix is lowercase + underscore, not uppercase+underscore — same word, different convention). Treat them as out-of-scope for D-3 (D-3 only mentions env vars).

Actually, re-reading CONTEXT D-3 verbatim: "Env var dual-read shim. MULTICA_* → ALGOPLAN_*". The cookies are NOT MULTICA_*. They are `multica_*` (lowercase). Different surface. They stay.

Document this distinction explicitly in the SUMMARY: "Plan 08-05 scope clarification: D-3 covers SCREAMING_CASE MULTICA_* env vars only. lowercase `multica_auth` / `multica_csrf` cookie names + `multica_signup_source` cookie are NOT renamed in this plan to avoid mid-deploy session loss. Schedule cookie rename for v0.6.0 with explicit user-comm + backend dual-read of both names during transition."

<!-- docker-compose.selfhost.yml env vars (verified): -->
- ${MULTICA_BACKEND_IMAGE:-...}, ${MULTICA_WEB_IMAGE:-...}, ${MULTICA_IMAGE_TAG:-latest}, ${MULTICA_APP_URL:-...}
- All four → ALGOPLAN_* equivalents
- Default values: this plan keeps the ghcr image NAMES as `multica-backend` / `multica-web` (Plan 08-07 flips those to `algoplan-backend` / `algoplan-web`)

<!-- Makefile env-vars (verified): -->
- MULTICA_APP_URL ?= ... (line 18)
- MULTICA_SERVER_URL ?= ... (line 23)
- MULTICA_ARGS ?= ... (line 27)
- All three → ALGOPLAN_* equivalents

<!-- .env.example (verified — lines 26-44, 52): -->
- MULTICA_SERVER_URL, MULTICA_APP_URL, MULTICA_DAEMON_*, MULTICA_CODEX_*, MULTICA_IMAGE_TAG, MULTICA_BACKEND_IMAGE, MULTICA_WEB_IMAGE
- Strategy: rename PRIMARY entries to ALGOPLAN_*, ADD a "Deprecated (keep for one release)" comment block listing the legacy MULTICA_* names with a sunset target.

<!-- turbo.json globalEnv (line 10): MULTICA_SERVER_URL → ALGOPLAN_SERVER_URL -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Go env-var call sites to use config.GetEnv("ALGOPLAN_X") and update FlagOrEnv to dual-read internally</name>
  <files>server/internal/cli/flags.go, server/internal/daemon/config.go, server/cmd/multica/cmd_agent.go, server/cmd/multica/cmd_auth.go, server/cmd/multica/cmd_login.go, server/cmd/multica/cmd_issue.go, server/cmd/multica/cmd_workspace.go, server/cmd/multica/cmd_autopilot.go, server/cmd/multica/cmd_repo.go, server/cmd/multica/cmd_daemon.go, server/cmd/multica/cmd_agent_test.go, server/cmd/multica/cmd_auth_test.go, server/cmd/multica/main.go, server/cmd/multica/help.go, server/internal/handler/skill_test.go</files>
  <read_first>
    - server/internal/cli/flags.go (full file — FlagOrEnv signature + impl)
    - server/internal/daemon/config.go (full file — envOrDefault helper + all env reads)
    - server/cmd/multica/cmd_agent.go (full file — most MULTICA_* references)
    - server/cmd/multica/help.go (env-var documentation block)
    - server/cmd/multica/main.go (flag persistent flag descriptions)
    - server/internal/config/env.go (Plan 08-01 output — confirms config.GetEnv signature)
  </read_first>
  <action>
**Step 1 — Update server/internal/cli/flags.go to internally use config.GetEnv:**

Read the current FlagOrEnv implementation. It likely calls `os.Getenv(envName)`. Replace that with:

```go
package cli

import (
    "github.com/multica-ai/multica/server/internal/config"
    // ... existing imports
)

// FlagOrEnv returns the flag value when set, else the env var (via the
// dual-read shim in server/internal/config), else defaultValue.
//
// envName MUST start with "ALGOPLAN_" — config.GetEnv panics otherwise.
// The legacy MULTICA_-prefixed equivalent is read automatically with a
// one-shot deprecation warning per process per variable.
func FlagOrEnv(cmd *cobra.Command, flagName, envName, defaultValue string) string {
    if flag := cmd.Flag(flagName); flag != nil && flag.Changed {
        // existing flag-read path unchanged
    }
    if v := config.GetEnv(envName); v != "" {
        return v
    }
    return defaultValue
}
```

Preserve the EXACT existing code path for `flag.Changed` reads — only replace the `os.Getenv(envName)` line with `config.GetEnv(envName)`. If FlagOrEnv currently has a different shape (e.g. takes a cobra.Flag *Flag instead of FlagSet), preserve that and substitute only the os.Getenv line.

**Step 2 — Sweep all `os.Getenv("MULTICA_X")` and `cli.FlagOrEnv(..., "MULTICA_X", ...)` to ALGOPLAN_X:**

Generate the complete file list:
```bash
grep -rln "MULTICA_" server/cmd server/internal --include="*.go" 2>/dev/null
```

For EACH file in the list, do mechanical substitution `MULTICA_` → `ALGOPLAN_` (whole-word; `MULTICA_` always means env-var name in Go code, never an identifier). Use perl for the bulk edit, same approach as Plan 08-03 Task 2:

```bash
while IFS= read -r f; do
  perl -i -pe 's|MULTICA_|ALGOPLAN_|g' "$f"
done < <(grep -rln "MULTICA_" server/cmd server/internal --include="*.go")
```

After substitution, manually inspect:
- `cmd_agent.go` — verify all `os.Getenv("ALGOPLAN_*")` calls are still inside `func`s where importing config is acceptable; consider whether to keep `os.Getenv` (which DOES NOT route through the shim) or convert to `config.GetEnv`. The choice depends on whether the variable is hot-path or boot-time:
  - `cmd_agent.go` lines 170, 173 (MULTICA_AGENT_ID, MULTICA_TASK_ID) — read at function entry, infrequent → keep `os.Getenv("ALGOPLAN_*")` (no shim → no fallback to legacy MULTICA_AGENT_ID). HOWEVER, this means a self-hoster running an agent with `MULTICA_AGENT_ID=...` set in their daemon's task env will see the variable as unset.
  - **Decision rule:** for SELF-HOSTER-OWNED env vars (those documented in `.env.example` for self-hosting), USE `config.GetEnv` (so dual-read works). For DAEMON-INTERNAL env vars (set by our own daemon code when spawning agent processes), use plain `os.Getenv("ALGOPLAN_X")` because the daemon code that SETS them is in the same release as the code that READS them — no compat needed.
  - Self-hoster vars: ALGOPLAN_SERVER_URL, ALGOPLAN_APP_URL, ALGOPLAN_BACKEND_IMAGE, ALGOPLAN_WEB_IMAGE, ALGOPLAN_IMAGE_TAG, ALGOPLAN_DAEMON_*, ALGOPLAN_CODEX_*, ALGOPLAN_CLAUDE_*, ALGOPLAN_OPENCODE_*, ALGOPLAN_OPENCLAW_*, ALGOPLAN_HERMES_*, ALGOPLAN_TOKEN, ALGOPLAN_RESEND_*, ALGOPLAN_RUN_SKILLS_SH_INTEGRATION
  - Daemon-internal: ALGOPLAN_AGENT_ID, ALGOPLAN_TASK_ID, ALGOPLAN_WORKSPACE_ID, ALGOPLAN_AGENT_NAME, ALGOPLAN_AGENT_RUNTIME_NAME, ALGOPLAN_LAUNCHED_BY, ALGOPLAN_DAEMON_PORT — these are set by `server/internal/handler/daemon.go` when spawning agent tasks; daemon code is in same release as agent CLI

  For self-hoster vars: change `os.Getenv("ALGOPLAN_X")` → `config.GetEnv("ALGOPLAN_X")` and add `import "github.com/multica-ai/multica/server/internal/config"` to each file's import block.
  For daemon-internal: leave as `os.Getenv("ALGOPLAN_X")`.

**Step 3 — Update test files:**

Test files use `t.Setenv("MULTICA_X", ...)` to inject values. After Step 2, the production code reads `ALGOPLAN_X`. Tests must set BOTH or shift fully:

For `cmd_agent_test.go`, `cmd_auth_test.go`:
- `t.Setenv("MULTICA_X", v)` → `t.Setenv("ALGOPLAN_X", v)` (production now reads new name; test sets new name)
- The shim's dual-read DOES handle the legacy name, but tests should assert the PRIMARY contract (ALGOPLAN_X), not the deprecation path

ADD a single new integration test in `cmd_auth_test.go` (or a new `cmd_compat_test.go`) asserting backwards-compat:
```go
func TestAppURL_LegacyMulticaEnvVarStillWorks(t *testing.T) {
    config.ResetDeprecationWarnings()
    t.Setenv("ALGOPLAN_APP_URL", "")
    t.Setenv("MULTICA_APP_URL", "http://legacy.example")
    got := resolveAppURL()  // or whichever helper the prod code uses
    if got != "http://legacy.example" {
        t.Fatalf("expected dual-read fallback; got %q", got)
    }
}
```

This test proves the deprecation shim is wired end-to-end (not just unit-tested in Plan 08-01).

**Step 4 — Update help.go env-var documentation block:**

Lines 128-129 in `server/cmd/multica/help.go` document env vars to users. Change:
```
  ALGOPLAN_SERVER_URL    Override the default server URL
  ALGOPLAN_WORKSPACE_ID  Set the active workspace
```

ADD a deprecation note:
```
  Note: legacy MULTICA_-prefixed names (MULTICA_SERVER_URL, etc.) still work
  with a deprecation warning. Plan to remove in v0.6.0 or v0.7.0.
```

**Step 5 — Build and test:**

```bash
cd server && go build ./...
cd server && go vet ./...
cd server && go test ./cmd/multica/... ./internal/cli/... ./internal/config/... ./internal/daemon/... -count=1
```

All tests should pass. The new `TestAppURL_LegacyMulticaEnvVarStillWorks` proves end-to-end dual-read.

If `go vet` flags unused imports of `config` package in any file (because the call site now uses `os.Getenv` with the daemon-internal rule), remove that import. Goimports would auto-fix.
  </action>
  <verify>
    <automated>cd server && go build ./... && go vet ./... && go test ./cmd/multica/... ./internal/cli/... ./internal/config/... ./internal/daemon/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero `MULTICA_` substrings in `server/cmd` and `server/internal` Go source files (verify with `! grep -rln "MULTICA_" server/cmd server/internal --include="*.go" 2>/dev/null`)
    - `server/internal/cli/flags.go` imports `config` package (verify `grep "internal/config" server/internal/cli/flags.go` returns >= 1)
    - `server/cmd/multica/cmd_auth.go`, `cmd_login.go` use `config.GetEnv` for ALGOPLAN_APP_URL (verify `grep -E "config\\.GetEnv\\(\"ALGOPLAN_APP_URL\"\\)" server/cmd/multica/cmd_auth.go server/cmd/multica/cmd_login.go | wc -l` returns >= 2)
    - All Go test files use `t.Setenv("ALGOPLAN_*", ...)` (verify with `! grep "t.Setenv(\"MULTICA_" server/cmd/multica/*_test.go server/internal/handler/*_test.go`)
    - At least one test asserts legacy MULTICA_* env still works via shim (verify `grep -E "MULTICA_APP_URL.*legacy|legacy.*MULTICA" server/cmd/multica/cmd_auth_test.go` returns >= 1, OR a new compat test exists)
    - `cd server && go build ./...` exits 0
    - `cd server && go vet ./...` exits 0
    - `cd server && go test ./cmd/multica/... ./internal/cli/... ./internal/config/... -count=1` exits 0
  </acceptance_criteria>
  <done>
    All Go env reads use ALGOPLAN_ names. Self-hoster vars route through config.GetEnv (dual-read). Daemon-internal vars use os.Getenv directly (single source of truth, same release). Help text and tests are consistent. Backwards-compat verified end-to-end.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docker-compose, Makefile, .env.example, turbo.json env-name references</name>
  <files>docker-compose.selfhost.yml, docker-compose.selfhost.build.yml, Makefile, .env.example, turbo.json</files>
  <read_first>
    - docker-compose.selfhost.yml (full file — env block + image refs)
    - docker-compose.selfhost.build.yml (full file)
    - Makefile (lines 1-100, 250-310 — env-var blocks and CLI helper targets)
    - .env.example (full file)
    - turbo.json (full file — globalEnv array)
  </read_first>
  <action>
**docker-compose.selfhost.yml** — env-var name flips:
- `${MULTICA_BACKEND_IMAGE:-ghcr.io/multica-ai/multica-backend}` → `${ALGOPLAN_BACKEND_IMAGE:-ghcr.io/multica-ai/multica-backend}`
- `${MULTICA_WEB_IMAGE:-ghcr.io/multica-ai/multica-web}` → `${ALGOPLAN_WEB_IMAGE:-ghcr.io/multica-ai/multica-web}`
- `${MULTICA_IMAGE_TAG:-latest}` (multiple occurrences) → `${ALGOPLAN_IMAGE_TAG:-latest}`
- `MULTICA_APP_URL: ${MULTICA_APP_URL:-http://localhost:3000}` → `ALGOPLAN_APP_URL: ${ALGOPLAN_APP_URL:-http://localhost:3000}`

**IMPORTANT** — keep the default-value image NAME suffix as `multica-backend` / `multica-web` for now. Plan 08-07 owns the rename to `algoplan-backend` / `algoplan-web`. This plan ONLY renames the *variable names*.

**docker-compose.selfhost.build.yml** — same pattern; flip variable references but keep defaults as-is.

**Makefile** — flip variable definitions:
- Line 18: `MULTICA_APP_URL ?= $(FRONTEND_ORIGIN)` → `ALGOPLAN_APP_URL ?= $(FRONTEND_ORIGIN)`
- Line 23: `MULTICA_SERVER_URL ?= ws://localhost:$(PORT)/ws` → `ALGOPLAN_SERVER_URL ?= ws://localhost:$(PORT)/ws`
- Line 27: `MULTICA_ARGS ?= $(ARGS)` → `ALGOPLAN_ARGS ?= $(ARGS)`
- Line 90, 91 (echo lines): `$${MULTICA_BACKEND_IMAGE...}` / `$${MULTICA_WEB_IMAGE...}` / `$${MULTICA_IMAGE_TAG...}` → `$${ALGOPLAN_BACKEND_IMAGE...}` / `$${ALGOPLAN_WEB_IMAGE...}` / `$${ALGOPLAN_IMAGE_TAG...}`
- Line 70: `$${MULTICA_IMAGE_TAG:-latest}` → `$${ALGOPLAN_IMAGE_TAG:-latest}`
- Line 267: `MULTICA_ARGS=` → `ALGOPLAN_ARGS=`
- Line 269 (`cli` target description): `Run the multica CLI with ARGS or MULTICA_ARGS` → `Run the multica CLI with ARGS or ALGOPLAN_ARGS` (note: the binary is still `multica` until Plan 08-06; we're only renaming env vars here)
- Line 270: `MULTICA_ARGS=...` → `ALGOPLAN_ARGS=...`
- Line 273: `cd server && go run ./cmd/multica $(MULTICA_ARGS)` → `cd server && go run ./cmd/multica $(ALGOPLAN_ARGS)` (binary path stays cmd/multica until Plan 08-06)

After all Makefile edits, run `make help` and verify it doesn't error.

**.env.example** — restructure with deprecation header:

Read the full file. Edit to flip all `MULTICA_*` lines to `ALGOPLAN_*`. ADD a header block:
```
# AlgoPlan environment variables.
# All variables use ALGOPLAN_ prefix as of v0.5.0.
#
# DEPRECATION: Variables previously named MULTICA_* still work via a
# dual-read shim with a one-shot warning. Schedule for removal in v0.6.0
# or v0.7.0.
```

Flip:
- Line 26: `MULTICA_SERVER_URL=ws://localhost:8080/ws` → `ALGOPLAN_SERVER_URL=ws://localhost:8080/ws`
- Line 27: `MULTICA_APP_URL=http://localhost:3000` → `ALGOPLAN_APP_URL=http://localhost:3000`
- Lines 28-37: all `MULTICA_DAEMON_*`, `MULTICA_CODEX_*` → `ALGOPLAN_*`
- Lines 43-45: `MULTICA_IMAGE_TAG`, `MULTICA_BACKEND_IMAGE`, `MULTICA_WEB_IMAGE` → `ALGOPLAN_*`
- Line 52: `RESEND_FROM_EMAIL=noreply@multica.ai` — leave as-is. Plan 08-07 owns this.

**turbo.json** — globalEnv array (line 10):
- `"MULTICA_SERVER_URL"` → `"ALGOPLAN_SERVER_URL"`

**Verification** — run end-to-end:
- `make help` exits 0
- `make test` (or `cd server && go test ./...`) exits 0
- `docker compose -f docker-compose.selfhost.yml config` exits 0 (validates compose YAML)

If a self-hoster has `.env` (NOT `.env.example`) in their checkout with `MULTICA_*` names, they remain unchanged on disk by this plan (we only edit `.env.example` which is the template). The Plan 08-01 shim ensures their existing `.env` keeps working.
  </action>
  <verify>
    <automated>! grep -rln "MULTICA_" docker-compose.selfhost.yml docker-compose.selfhost.build.yml Makefile .env.example turbo.json 2>/dev/null && make help > /dev/null && docker compose -f docker-compose.selfhost.yml config > /dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Zero `MULTICA_` substrings in `docker-compose.selfhost.yml`, `docker-compose.selfhost.build.yml`, `Makefile`, `.env.example`, `turbo.json` (verify with `! grep -rln "MULTICA_" docker-compose.selfhost.yml docker-compose.selfhost.build.yml Makefile .env.example turbo.json 2>/dev/null`)
    - `.env.example` contains the deprecation header (verify with `grep -E "DEPRECATION:.*MULTICA_" .env.example` returns >= 1)
    - `turbo.json` globalEnv contains `"ALGOPLAN_SERVER_URL"` (verify with `grep "ALGOPLAN_SERVER_URL" turbo.json` returns >= 1)
    - `Makefile` uses `ALGOPLAN_ARGS` (verify with `grep -c "ALGOPLAN_ARGS" Makefile` returns >= 3)
    - `make help` exits 0
    - `docker compose -f docker-compose.selfhost.yml config` exits 0 (compose YAML validates)
    - The default ghcr image NAMES `multica-backend` / `multica-web` are still present in compose defaults (Plan 08-07 owns those — verify `grep "multica-backend" docker-compose.selfhost.yml` returns >= 1; this is INTENTIONAL preservation)
  </acceptance_criteria>
  <done>
    docker-compose, Makefile, .env.example, turbo.json all use ALGOPLAN_ env-var names. Image NAME defaults stay as multica-* (Plan 08-07 ownership). Self-hosters with existing .env files keep working via Plan 08-01 shim. Documentation explicitly tells new self-hosters about the deprecation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS env → CLI/server | Existing trust boundary; shim does not change posture |
| docker-compose env block → container env | Compose passes the renamed variables; container reads via Go shim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | self-hoster has both legacy and new env vars set with different values | mitigate | Plan 08-01 shim's prefer-new rule means the new ALGOPLAN_X wins; legacy MULTICA_X is silently ignored. Documented in .env.example deprecation header so users know which one takes precedence |
| T-08-05-02 | Information Disclosure | help.go env documentation reveals new variable names too eagerly | accept | Variable NAMES are not secrets; they're documented at scope already |
| T-08-05-03 | Denial of Service | tests setting MULTICA_X linger across packages and trigger warnings during test runs | mitigate | Test files migrated to set ALGOPLAN_X; new compat test explicitly tests the legacy path with config.ResetDeprecationWarnings() bookends to avoid cross-test pollution |
</threat_model>

<verification>
- `cd server && go build ./...` exits 0
- `cd server && go vet ./...` exits 0
- `cd server && go test ./...` exits 0 (all subsystems pass)
- `make help` exits 0
- `docker compose -f docker-compose.selfhost.yml config` exits 0
- Zero `MULTICA_` substrings in Go source, Makefile, docker-compose, .env.example, turbo.json
- New compat test asserts MULTICA_APP_URL legacy fallback still works end-to-end
</verification>

<success_criteria>
1. All Go call sites read ALGOPLAN_* (via config.GetEnv for self-hoster vars; via os.Getenv for daemon-internal vars)
2. cli.FlagOrEnv internally uses config.GetEnv — single source of truth for dual-read
3. docker-compose, Makefile, .env.example, turbo.json all use ALGOPLAN_* names
4. .env.example documents the deprecation
5. End-to-end test verifies legacy MULTICA_* env-var fallback through the shim
6. Cookie names (`multica_auth`, `multica_csrf`, `multica_signup_source`) are EXPLICITLY out of scope (documented in SUMMARY for v0.6.0)
7. Image NAME suffixes (`multica-backend`/`multica-web`) are EXPLICITLY out of scope (Plan 08-07)
</success_criteria>

<output>
After completion, create `.planning/phases/08-internal-rebrand-completion/08-05-SUMMARY.md`
</output>
