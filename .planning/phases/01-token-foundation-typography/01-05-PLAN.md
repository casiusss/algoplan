---
phase: 01-token-foundation-typography
plan: 05
type: execute
wave: 2
depends_on: ["01-00"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [FND-04]
tags: [docs, requirements-update, scope-change, d-19]

must_haves:
  truths:
    - "REQUIREMENTS.md no longer lists FND-04 in the active Foundation requirements section"
    - "REQUIREMENTS.md Out of Scope table includes a row for the dropped FND-04 CI rule with the user-supplied rationale"
    - "REQUIREMENTS.md Traceability table no longer references FND-04 in the Phase 1 row, and the v1 coverage count is decremented (51 → 50)"
    - "ROADMAP.md Phase 1 Success Criteria has criterion #4 either removed OR rewritten as a one-time post-Phase-1 manual grep check (no CI enforcement)"
    - "ROADMAP.md Phase 1 Requirements line no longer lists FND-04 (now: FND-01, FND-02, FND-03)"
    - "PROJECT.md 'What This Is' updates the obsolete 'deep-forest-green sidebar + mint-sage canvas + white cards' description to reflect the Algorivo OKLCH palette direction"
    - "PROJECT.md Active Requirements → Foundation & Tokens reflects the locked Algorivo direction (no mint-sage)"
    - "PROJECT.md Key Decisions table gains an entry recording the FND-04 drop with date and rationale"
    - "STATE.md Deferred Items 'Source_Serif_4 removal audit' row is updated to reflect the partial-removal decision (italic axis only — base import + token kept; full removal blocked by 14 onboarding consumers)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FND-04 dropped from active requirements; appears in Out of Scope; coverage count = 50"
      contains: "FND-04"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 Requirements line and Success Criteria reflect FND-04 drop"
      contains: "Requirements:** FND-01, FND-02, FND-03"
    - path: ".planning/PROJECT.md"
      provides: "What This Is + Foundation & Tokens reflect Algorivo direction; Key Decisions row added"
      contains: "Algorivo"
    - path: ".planning/STATE.md"
      provides: "Source_Serif_4 audit deferred-item row updated to 'partial removal — italic axis only' with link to Plan 02 SUMMARY"
      contains: "partial removal"
  key_links:
    - from: ".planning/REQUIREMENTS.md Foundation section"
      to: ".planning/REQUIREMENTS.md Out of Scope table"
      via: "FND-04 row migrated; rationale references CONTEXT D-19"
      pattern: "FND-04"
    - from: ".planning/ROADMAP.md Phase 1 Requirements"
      to: ".planning/REQUIREMENTS.md FND-01..03"
      via: "Three-ID list (FND-04 removed); roadmap and requirements stay in sync"
      pattern: "FND-01, FND-02, FND-03"
---

<objective>
Update the four planning documents to reflect the user-locked decision in CONTEXT D-19: the FND-04 CI rule (hardcoded Tailwind color enforcement) is dropped from scope. The migration of existing violations is still happening (in Plan 04), but no automated CI rule will guard against future violations.

Also update PROJECT.md to reflect the obsolete mint-sage palette description (CONTEXT scope-change note: palette source shifted from PROJECT.md mock to Algorivo web), and STATE.md to reflect the partial Source_Serif_4 removal decision (planner Q4 deviation from D-12).

This plan is the documentation-side counterpart to the code-side work. It runs in Wave 2 because:
- It doesn't depend on any of Plans 01-04 finishing first (the docs reflect decisions, not code outcomes).
- It DOES depend on Plan 00 only because the SUMMARY of this plan references Wave 0 deliverables.
- Putting it in Wave 2 ensures it's not racing with Plan 04's potential migration discoveries that might warrant requirements updates.

Purpose: Project documentation is the contract between phases. If REQUIREMENTS.md still lists FND-04 as active when nobody implements the CI rule, future phases plan against false coverage. The downstream gsd-checker / gsd-verifier read these files to validate work.

Output: 4 documentation files updated. No code touched.

Locked planner decisions resolved here:
- D-19 — FND-04 dropped per user. This plan implements steps 1-3 of D-19 (REQUIREMENTS, ROADMAP, Traceability). Step 4 (Phase 1 success criteria 1-3 stay) is honored by NOT touching them.
- CONTEXT scope-change note (palette source) — PROJECT.md updated to Algorivo direction.
- Q4 (Source_Serif_4 partial removal) — STATE.md deferred-items row updated to reflect that only the italic axis was removed; full removal blocked by onboarding consumers (14 lines, 7 files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-token-foundation-typography/01-CONTEXT.md
@.planning/phases/01-token-foundation-typography/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Update REQUIREMENTS.md — drop FND-04 to Out of Scope</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — confirms FND-04 location at line 15, Out of Scope table location, Traceability table at lines ~158-173)
    - .planning/phases/01-token-foundation-typography/01-CONTEXT.md §"D-19"
  </read_first>
  <action>
Apply THREE coordinated edits to `.planning/REQUIREMENTS.md`:

**Edit 1 — Remove FND-04 from active Foundation list (line 15):**

Find:
```markdown
- [ ] **FND-04**: CI-Regel erkennt hardcoded Tailwind-Farben (`text-red-500`, `bg-yellow-200` etc.) in `packages/views/` und `packages/ui/` und schlägt fehl — bestehende 6 Verstöße werden migriert
```

Delete this entire line (and its preceding blank line if it leaves a double-blank).

The Foundation section should now contain only FND-01, FND-02, FND-03.

**Edit 2 — Add FND-04 row to Out of Scope table:**

Locate the `## Out of Scope` table (around line 130). Insert a new row at the bottom of the existing rows (or alphabetized — pick whichever the file currently uses; from the read, it appears to be order-of-decision, so append):

```markdown
| FND-04 CI-Regel gegen hardcoded Tailwind-Farben | User explicit dropped enforcement (CONTEXT D-19, 2026-04-24) — "keine — unnötig". Bestehende Verstöße werden Phase 1 Plan 04 als one-shot Migration entfernt; keine Lint-/CI-Regel verhindert neue Einführungen. Re-evaluate if Phase 2+ regressions appear. |
```

**Edit 3 — Update Traceability table (line 161):**

Find the Phase 1 row:
```markdown
| FND-01 → FND-04 | Phase 1 | Pending |
```

Replace with:
```markdown
| FND-01 → FND-03 | Phase 1 | Pending |
```

**Edit 4 — Update Coverage count (line 173):**

Find:
```markdown
- v1 requirements: 51 total (FND×4, UI×4, SB×4, SHL×5, KBN×7, DTL×4, AUTH×6, INB×3, SET×3, WS×5, RBR×6)
- Mapped to phases: 51/51 — verified by roadmapper 2026-04-24
```

Replace with:
```markdown
- v1 requirements: 50 total (FND×3, UI×4, SB×4, SHL×5, KBN×7, DTL×4, AUTH×6, INB×3, SET×3, WS×5, RBR×6) — FND-04 dropped to Out of Scope per CONTEXT D-19 (2026-04-24)
- Mapped to phases: 50/50 — verified by Phase 1 planner 2026-04-25
```

**Edit 5 — Bump the Last updated line at the bottom of the file:**

Find:
```markdown
*Last updated: 2026-04-24 — Roadmap created, traceability verified*
```

Replace with:
```markdown
*Last updated: 2026-04-25 — FND-04 dropped to Out of Scope per Phase 1 CONTEXT D-19; coverage count 51 → 50*
```

**Constraints:**
- Preserve all other requirement IDs (UI-01..04, SB-01..04, etc.) exactly.
- Preserve table column structure — do NOT add or remove columns.
- Preserve existing Out of Scope rows above the new FND-04 row.
- Keep German prose where it exists (REQUIREMENTS.md is German-mixed; the new Out of Scope row matches the German style of existing rows).
  </action>
  <verify>
    <automated>! grep -q "FND-04.*CI-Regel.*Tailwind" .planning/REQUIREMENTS.md | head -1; grep -q "FND-04 CI-Regel" .planning/REQUIREMENTS.md && grep -q "FND-01 → FND-03" .planning/REQUIREMENTS.md && grep -q "v1 requirements: 50 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - FND-04 no longer appears as an active checkbox item in the Foundation section.
    - FND-04 appears as a row in the Out of Scope table with rationale referencing CONTEXT D-19.
    - Traceability table Phase 1 row reads "FND-01 → FND-03".
    - Coverage count reads "50 total" / "50/50".
    - Last-updated line bumped to 2026-04-25 with the change reason.
  </done>
</task>

<task type="auto">
  <name>Task 5.2: Update ROADMAP.md — Phase 1 Requirements line + Success Criterion #4</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (entire file — Phase 1 block is at lines 25-34)
    - .planning/phases/01-token-foundation-typography/01-CONTEXT.md §"D-19" step 2
  </read_first>
  <action>
Apply TWO edits to `.planning/ROADMAP.md`:

**Edit 1 — Phase 1 Requirements line (line 28):**

Find:
```markdown
**Requirements**: FND-01, FND-02, FND-03, FND-04
```

Replace with:
```markdown
**Requirements**: FND-01, FND-02, FND-03
```

**Edit 2 — Phase 1 Success Criterion #4 (lines 33):**

Find:
```markdown
  4. Running `grep -rn "text-red-\|bg-blue-\|text-green-" packages/views packages/ui` returns zero results — CI rule enforces this and fails on any new violation
```

Replace with:
```markdown
  4. Running `bash scripts/grep-hardcoded-colors.sh` returns zero results — one-shot post-Phase-1 manual verification (no CI rule per CONTEXT D-19; future regressions are caught at PR review only)
```

**Optionally** (planner's discretion — pick one of the following):
- Keep the rewritten criterion #4 as a one-shot manual check (recommended; preserves the goal).
- OR delete it entirely and renumber criteria #1-3 (NOT recommended; the migration target is real and worth verifying once).

Use the rewrite (option 1).

**Edit 3 — Plans line (line 34):**

If the Plans line currently says `**Plans**: TBD`, leave it for now. The orchestrator update_roadmap step (in the planner's standard flow) will populate the actual plan list (`01-01-PLAN.md`, `01-02-PLAN.md`, `01-03-PLAN.md`, `01-04-PLAN.md`, `01-05-PLAN.md`) along with brief objectives. If this plan executes BEFORE that orchestrator step, leave Plans: TBD; if AFTER, leave whatever the orchestrator wrote.

**Constraints:**
- Do NOT modify other phases (Phase 2-7).
- Do NOT modify the Phase 1 Goal (it remains accurate after FND-04 drop).
- Do NOT modify Success Criteria #1, #2, #3.
  </action>
  <verify>
    <automated>grep -q "Requirements\*\*: FND-01, FND-02, FND-03$" .planning/ROADMAP.md && grep -q "scripts/grep-hardcoded-colors.sh" .planning/ROADMAP.md && ! grep -q "Requirements\*\*: FND-01, FND-02, FND-03, FND-04" .planning/ROADMAP.md && ! grep -q "CI rule enforces this" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - Phase 1 Requirements line reads "FND-01, FND-02, FND-03" (no FND-04).
    - Success Criterion #4 references `scripts/grep-hardcoded-colors.sh` and explicitly notes "no CI rule".
    - Phases 2-7 UNCHANGED.
    - Goal line UNCHANGED.
    - Success Criteria #1-3 UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 5.3: Update PROJECT.md — palette description + Key Decisions row</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (entire file — "What This Is" at lines 3-5, Active Requirements → Foundation & Tokens at lines 32-39, Key Decisions table at lines 134-145)
    - .planning/phases/01-token-foundation-typography/01-CONTEXT.md §"Phase Boundary" scope-change note + §"D-01" (Algorivo source)
  </read_first>
  <action>
Apply THREE edits to `.planning/PROJECT.md`:

**Edit 1 — "What This Is" section (line 5):**

Find:
```markdown
Kompletter Frontend-Redesign der Multica-Plattform inklusive Rebrand zu **AlgoPlan** (app-facing). Die bestehende Linear-artige Issue-Plattform wird visuell auf eine neue Board-zentrierte Identität umgestellt: deep-forest-green / mint-sage Palette, Inter als Schriftfamilie (inkl. italic für Headlines), neues Kanban-Board als alternative Issue-View, neuer Komponenten-Showroom. Code-Internals (Paketnamen `@multica/*`, DB-Schemas, CLI-Binary, Repo) bleiben unverändert — nur die User-facing Oberfläche wird getauscht.
```

Replace `deep-forest-green / mint-sage Palette` with `Algorivo OKLCH Palette (brand-green #008757 auf neutralem Surface, near-white #fafbfc light / near-black #0f1318 dark)`. Result:

```markdown
Kompletter Frontend-Redesign der Multica-Plattform inklusive Rebrand zu **AlgoPlan** (app-facing). Die bestehende Linear-artige Issue-Plattform wird visuell auf eine neue Board-zentrierte Identität umgestellt: Algorivo OKLCH Palette (brand-green #008757 auf neutralem Surface, near-white #fafbfc light / near-black #0f1318 dark), Inter als Schriftfamilie (inkl. italic für Headlines), neues Kanban-Board als alternative Issue-View, neuer Komponenten-Showroom. Code-Internals (Paketnamen `@multica/*`, DB-Schemas, CLI-Binary, Repo) bleiben unverändert — nur die User-facing Oberfläche wird getauscht.
```

**Edit 2 — Active Requirements → Foundation & Tokens (line 33):**

Find:
```markdown
- [ ] Neues Farbsystem (deep-forest-green Sidebar/Header, mint-sage Canvas, weiße Cards) via CSS-Variablen in `packages/ui/styles/`
```

Replace with:
```markdown
- [ ] Neues Farbsystem (Algorivo-derived OKLCH-Palette: brand-green #008757 primary, near-white/-black backgrounds, weiße/dark Cards) via CSS-Variablen in `packages/ui/styles/`
```

**Edit 3 — Key Decisions table (after line 137):**

Locate the Key Decisions table. Add a new row after the existing rows (the "Both Modes (Light + Dark)" row appears to be the last):

```markdown
| Algorivo OKLCH-Palette adoptiert (FND-01); FND-04 CI-Regel gedroppt (D-19) | Brand-direction-shift weg von mint-sage hin zu Algorivo brand-green; user explicit "CI-Regel unnötig" | — Pending Phase 1 ship |
```

Also if the table contains the row "Inter als alleinige Font-Familie", consider adding a parenthetical noting the partial Source_Serif_4 retention for landing/onboarding (planner Q4):

Find:
```markdown
| Inter als alleinige Font-Familie (inkl. italic für Headlines) | User hat Inter lokal evtl. nicht geladen, Mock zeigt serif-Fallback — echte Absicht ist Inter | — Pending |
```

Replace with:
```markdown
| Inter als alleinige Font-Familie (inkl. italic für Headlines); Source_Serif_4 nur für Landing + Onboarding behalten (Phase 1 planner Q4 deviation von D-12) | User hat Inter lokal evtl. nicht geladen, Mock zeigt serif-Fallback — echte Absicht ist Inter; Onboarding consumes font-serif (14 lines / 7 files), kann nicht ohne View-Redesign entfernt werden | — Pending Phase 1 ship; full Source_Serif_4 removal deferred to onboarding-redesign phase |
```

**Constraints:**
- Preserve all other PROJECT.md sections (Core Value, Validated requirements, Out of Scope, Context, Constraints, Evolution).
- Preserve German prose style.
- Do NOT touch the "Phase-Timeline-Bar" / "Effort-Segmented-Control" / "Launch-Blocker-Toggle" out-of-scope rows; they are independent of FND-04.
- The Foundation section already lists 5 bullets — only edit the first one (Farbsystem). Leave the others (Inter Font, Light/Dark, semantic tokens, colored chips, avatars) intact.
  </action>
  <verify>
    <automated>grep -q "Algorivo OKLCH Palette" .planning/PROJECT.md && grep -q "brand-green #008757" .planning/PROJECT.md && ! grep -q "deep-forest-green / mint-sage" .planning/PROJECT.md && grep -q "Algorivo OKLCH-Palette adoptiert" .planning/PROJECT.md</automated>
  </verify>
  <done>
    - "What This Is" line replaces mint-sage with Algorivo OKLCH description.
    - Active Requirements → Foundation & Tokens first bullet updated to Algorivo direction.
    - Key Decisions table contains new row for Algorivo adoption + FND-04 drop.
    - Inter font row optionally extended with Source_Serif_4 partial-retention rationale.
    - All other PROJECT.md sections UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 5.4: Update STATE.md — Source_Serif_4 audit deferred-item status</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (entire file — Deferred Items table is around lines 79-86)
    - .planning/phases/01-token-foundation-typography/01-CONTEXT.md §"D-12" + planner Q4 deviation
  </read_first>
  <action>
Edit `.planning/STATE.md` Deferred Items table.

Find:
```markdown
| Open question | `Source_Serif_4` removal | Audit for remaining uses before removing in Phase 1 | Roadmap creation |
```

Replace with:
```markdown
| Partial closure | `Source_Serif_4` removal | PARTIAL: italic axis removed from desktop (Phase 1 Plan 02 Task 2.2). Base import + `--font-serif` token KEPT — onboarding (`packages/views/onboarding/**`) consumes `font-serif` (14 lines / 7 files). Full removal deferred to onboarding-redesign phase. | Roadmap creation; partial closure 2026-04-25 (Phase 1 planner Q4 deviation from D-12) |
```

**ALSO update the Last activity line and last_updated frontmatter:**

In the frontmatter (line 7):
```yaml
last_updated: "2026-04-24T21:29:04.726Z"
```

Replace with the current date/time (use the date the plan executes; use ISO 8601 format like `"2026-04-25T00:00:00.000Z"` if the executor doesn't have a precise timestamp source).

In the Current Position section (line 31):
```markdown
Last activity: 2026-04-24 — Roadmap created; all 51 v1 requirements mapped across 7 phases
```

Replace with:
```markdown
Last activity: 2026-04-25 — Phase 1 plans created (5 plans across 2 waves + Wave 0 scaffolds); FND-04 dropped per CONTEXT D-19; Algorivo palette locked
```

**Constraints:**
- Preserve all other STATE.md content (Performance Metrics, Accumulated Context decisions, Pending Todos, Blockers/Concerns, Session Continuity).
- Do NOT modify the progress object in frontmatter (`progress.total_plans`, `progress.completed_plans`) — those track execution, not planning.
- Do NOT add or remove deferred-item rows besides the Source_Serif_4 update.
  </action>
  <verify>
    <automated>grep -q "Partial closure" .planning/STATE.md && grep -q "italic axis removed" .planning/STATE.md && grep -q "onboarding-redesign phase" .planning/STATE.md && grep -q "Phase 1 plans created" .planning/STATE.md</automated>
  </verify>
  <done>
    - Source_Serif_4 deferred item row updated to "Partial closure" with detailed rationale.
    - last_updated frontmatter bumped.
    - Last activity line reflects Phase 1 planning completion.
    - All other deferred-item rows UNCHANGED.
    - Performance Metrics, Decisions, Pending Todos, Blockers UNCHANGED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation file → planner / checker / verifier reads | Downstream agents read these files to validate code; stale docs lead to false-positive verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Documentation drift between PROJECT.md and CONTEXT.md | mitigate | Plan 05 explicitly synchronizes PROJECT.md to CONTEXT.md decisions. Future drift caught by /gsd-discuss-phase reading both. |
| T-01-05-02 | Repudiation | "Why was FND-04 dropped?" lost to history | mitigate | Each edit references CONTEXT D-19 explicitly with the date. Audit trail preserved across REQUIREMENTS, ROADMAP, PROJECT. |
| T-01-05-03 | Information Disclosure | None — these are internal planning docs |  accept  | Repo write controls already in place. |
</threat_model>

<verification>
After Plan 05 ships:

1. `grep -c "FND-04" .planning/REQUIREMENTS.md` returns at least 1 (the Out of Scope row) but NOT in the active Foundation list.
2. `.planning/ROADMAP.md` Phase 1 Requirements line reads exactly `**Requirements**: FND-01, FND-02, FND-03`.
3. `.planning/PROJECT.md` no longer mentions "deep-forest-green / mint-sage" in "What This Is".
4. `.planning/STATE.md` Source_Serif_4 row marked "Partial closure".
5. All other planning docs UNCHANGED.
6. No code touched (`git diff -- 'apps/' 'packages/' 'server/' 'e2e/'` returns empty).
</verification>

<success_criteria>
- 4 doc files updated.
- All edits traceable to CONTEXT D-19 (or D-12 + planner Q4 for STATE.md).
- Coverage count consistent across REQUIREMENTS.md (50/50) and Traceability table.
- Phase 1 Requirements list consistent across ROADMAP.md and the Phase 1 plan frontmatters (FND-01, FND-02, FND-03 + FND-04-MIGRATION + FND-04 documentation tracking).
- No code changes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-foundation-typography/01-05-SUMMARY.md` documenting:
- Each doc file's changes (one-line description per file)
- Confirmation that REQUIREMENTS coverage count went from 51 to 50
- Confirmation that ROADMAP Phase 1 Requirements line is now 3 IDs (not 4)
- Confirmation that STATE Source_Serif_4 row is "Partial closure"
- Note: Phase 1 Plan 04 (FND-04-MIGRATION) is the code-side correlate of this plan's documentation update — the migration happens in code, the CI rule is dropped in docs
</output>
