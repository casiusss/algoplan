---
phase: 02-atomic-ui-primitives
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/vitest.config.ts
  - packages/ui/test/setup.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Running pnpm --filter @multica/ui test exits 0 with no test files (or reports the smoke test green)"
    - "Vitest, jsdom, @vitejs/plugin-react, and the three @testing-library packages are installed in packages/ui devDependencies via catalog refs"
    - "vitest.config.ts loads ./test/setup.ts and uses jsdom environment with globals enabled"
    - "test/setup.ts imports @testing-library/jest-dom and shims matchMedia + ResizeObserver + elementFromPoint + memory localStorage so Base UI primitives can mount"
  artifacts:
    - path: "packages/ui/vitest.config.ts"
      provides: "Vitest test runner config (jsdom + plugin-react)"
      min_lines: 10
    - path: "packages/ui/test/setup.ts"
      provides: "jest-dom matchers + jsdom shims used by every Phase 2 test"
      min_lines: 50
    - path: "packages/ui/package.json"
      provides: "test script + test devDependencies"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "packages/ui/vitest.config.ts"
      to: "packages/ui/test/setup.ts"
      via: "setupFiles array"
      pattern: "setupFiles.*test/setup"
    - from: "packages/ui/package.json"
      to: "vitest, jsdom, @vitejs/plugin-react, @testing-library/*"
      via: "devDependencies catalog refs"
      pattern: "\"vitest\":\\s*\"catalog:\""
---

<objective>
Land the Vitest + jsdom + Testing Library infrastructure inside `packages/ui` so every Wave 1 / Wave 2 plan in Phase 2 can write co-located component tests. Mirrors the working blueprint from `packages/views`.

Purpose: `packages/ui` currently has zero test infrastructure (no `vitest.config.ts`, no `test/setup.ts`, no `test` script, no test devDeps). Without this Wave 0 step every subsequent plan would fail with "vitest: command not found" or "no test files found".

Output:
- `packages/ui/vitest.config.ts` (mirrors `packages/views/vitest.config.ts`)
- `packages/ui/test/setup.ts` (mirrors `packages/views/test/setup.ts`)
- `packages/ui/package.json` extended with `"test": "vitest run"` script + 6 catalog devDependencies
- Smoke verification that `pnpm --filter @multica/ui test` exits cleanly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-atomic-ui-primitives/02-RESEARCH.md
@.planning/phases/02-atomic-ui-primitives/02-VALIDATION.md
@.planning/phases/02-atomic-ui-primitives/02-PATTERNS.md
@CLAUDE.md
@packages/views/vitest.config.ts
@packages/views/test/setup.ts
@packages/views/package.json
@packages/ui/package.json
@pnpm-workspace.yaml

<interfaces>
<!-- Catalog refs from pnpm-workspace.yaml that this plan must use verbatim. -->
<!-- All six dev dependencies are already in the catalog — no new versions are introduced. -->

From pnpm-workspace.yaml (catalog block):
```yaml
vitest: "^4.1.0"
jsdom: "^29.0.1"
"@vitejs/plugin-react": "^6.0.1"
"@testing-library/react": "^16.3.2"
"@testing-library/jest-dom": "^6.9.1"
"@testing-library/user-event": "^14.6.1"
```

From packages/views/vitest.config.ts (the file to mirror, 12 lines):
```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./test/setup.ts"],
    include: ["**/*.test.{ts,tsx}"],
  },
});
```

From packages/views/test/setup.ts (the file to mirror — copy verbatim, 63 lines):
- imports `@testing-library/jest-dom/vitest`
- defines `createMemoryStorage()` and installs it on `globalThis.localStorage` + `window.localStorage`
- shims `window.matchMedia` (returns matches:false MediaQueryList)
- shims `globalThis.ResizeObserver` (no-op observe/unobserve/disconnect)
- shims `document.elementFromPoint` (returns null)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test devDependencies + test script to packages/ui/package.json</name>
  <files>packages/ui/package.json</files>
  <action>
Extend `packages/ui/package.json`:

1. Add to `scripts` block (after `"lint": "eslint ."`):
   ```json
   "test": "vitest run"
   ```

2. Add to `devDependencies` block (alphabetically merged with the existing entries `@multica/tsconfig`, `@types/linkify-it`, `@types/react`, `@types/react-dom`, `rehype-sanitize`, `typescript`):
   ```json
   "@testing-library/jest-dom": "catalog:",
   "@testing-library/react": "catalog:",
   "@testing-library/user-event": "catalog:",
   "@vitejs/plugin-react": "catalog:",
   "jsdom": "catalog:",
   "vitest": "catalog:"
   ```

All six entries MUST use the literal string `"catalog:"` (NOT a pinned version) — pnpm-workspace.yaml already defines all six. Do not run `pnpm add`; edit the JSON directly to keep the diff reviewable, then install.

After editing, run `pnpm install` from the repo root so pnpm resolves the catalog refs and writes `pnpm-lock.yaml`.
  </action>
  <verify>
    <automated>cd /Users/steph/dev/multica && pnpm install --frozen-lockfile=false &gt; /tmp/p2-00-install.log 2&gt;&amp;1 &amp;&amp; node -e "const p=require('./packages/ui/package.json'); const required=['vitest','jsdom','@vitejs/plugin-react','@testing-library/react','@testing-library/jest-dom','@testing-library/user-event']; const missing=required.filter(d=&gt;p.devDependencies?.[d]!=='catalog:'); if(missing.length){console.error('MISSING catalog refs:',missing);process.exit(1)} if(p.scripts?.test!=='vitest run'){console.error('test script missing');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>
- `packages/ui/package.json` contains `"test": "vitest run"` in `scripts`.
- All 6 test devDependencies present with literal `"catalog:"` value.
- `pnpm install` succeeds and `node_modules/.bin/vitest` exists under `packages/ui` (resolvable via `pnpm --filter @multica/ui exec which vitest`).
- No literal version strings introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create vitest.config.ts and test/setup.ts mirroring packages/views</name>
  <files>packages/ui/vitest.config.ts, packages/ui/test/setup.ts</files>
  <action>
Create two files. Both are byte-equivalent copies of the `packages/views` originals — no Phase 2-specific deltas.

**File 1: `packages/ui/vitest.config.ts`** — copy `packages/views/vitest.config.ts` verbatim:

```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./test/setup.ts"],
    include: ["**/*.test.{ts,tsx}"],
  },
});
```

**File 2: `packages/ui/test/setup.ts`** — copy `packages/views/test/setup.ts` verbatim (63 lines). Read the source file with the Read tool first, then Write the identical content. Do NOT abbreviate, do NOT remove the storage / matchMedia / ResizeObserver / elementFromPoint shims (every shim is required by Base UI primitives — see RESEARCH §Pitfall 4).

Why verbatim: PATTERNS.md classifies both files as "exact" analog matches. PATTERNS §Test File Patterns explicitly says "duplicating 63 lines beats over-abstracting before the second consumer materializes" — do not refactor into a shared package now.

After both files exist, smoke-run vitest from the package directory to confirm zero infra errors:
```
pnpm --filter @multica/ui exec vitest run --reporter=basic
```
This MUST exit 0 (it will print "No test files found" — that is the expected clean state at the end of Wave 0; Wave 1 plans add the first test file).
  </action>
  <verify>
    <automated>cd /Users/steph/dev/multica &amp;&amp; test -f packages/ui/vitest.config.ts &amp;&amp; test -f packages/ui/test/setup.ts &amp;&amp; node -e "const a=require('fs').readFileSync('packages/ui/vitest.config.ts','utf8'); const b=require('fs').readFileSync('packages/views/vitest.config.ts','utf8'); if(a.trim()!==b.trim()){console.error('vitest.config.ts diverges from packages/views');process.exit(1)} const sa=require('fs').readFileSync('packages/ui/test/setup.ts','utf8'); const sb=require('fs').readFileSync('packages/views/test/setup.ts','utf8'); if(sa.trim()!==sb.trim()){console.error('test/setup.ts diverges from packages/views');process.exit(1)} console.log('OK')" &amp;&amp; pnpm --filter @multica/ui exec vitest run --reporter=basic 2&gt;&amp;1 | tee /tmp/p2-00-smoke.log | grep -E "No test files found|0 passed" &gt; /dev/null</automated>
  </verify>
  <done>
- `packages/ui/vitest.config.ts` exists and is byte-equal (after trim) to `packages/views/vitest.config.ts`.
- `packages/ui/test/setup.ts` exists and is byte-equal (after trim) to `packages/views/test/setup.ts`.
- `pnpm --filter @multica/ui exec vitest run --reporter=basic` exits 0 (either "No test files found" or test runs cleanly).
- Wave 0 gate met: subsequent plans can add `*.test.ts(x)` files anywhere under `packages/ui` and they will be discovered automatically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev environment → test runner | jsdom interprets test fixtures only; no production data crosses this boundary |
| package.json → pnpm catalog | Catalog version refs are the only attack surface; pinned via pnpm-workspace.yaml hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | packages/ui/package.json devDependencies | mitigate | Use `catalog:` refs only; never pin literal versions; install via `pnpm install --frozen-lockfile=false` so pnpm-lock.yaml records the resolved hash |
| T-02-00-02 | Information Disclosure | test/setup.ts memory storage shim | accept | Shim is jsdom-scoped; never reaches production bundle (devDependency); identical pattern to `packages/views/test/setup.ts` already in repo |
| T-02-00-03 | Denial of Service | vitest config glob `**/*.test.{ts,tsx}` | accept | Pattern matches only files inside packages/ui/; jsdom + plugin-react are catalog-pinned and battle-tested |
</threat_model>

<verification>
- `pnpm install` succeeds with no warnings about peer dep resolution for the new test packages.
- `pnpm --filter @multica/ui exec vitest run --reporter=basic` exits 0.
- `pnpm typecheck` (root) still passes (the config file is `.ts` and must compile under the package's `tsconfig.json`).
</verification>

<success_criteria>
- All four artifacts in `must_haves.artifacts` exist on disk.
- The `key_links.pattern` regex matches in the named files (`setupFiles.*test/setup` in `vitest.config.ts`; `"vitest":\s*"catalog:"` in `package.json`).
- Wave 1 / Wave 2 plans can write `*.test.ts(x)` next to source files and `pnpm --filter @multica/ui test` discovers + runs them.
- No new dependency versions added — only catalog references.
</success_criteria>

<output>
After completion, create `.planning/phases/02-atomic-ui-primitives/02-00-SUMMARY.md`.
</output>
