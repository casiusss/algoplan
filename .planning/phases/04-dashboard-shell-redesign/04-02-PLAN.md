---
phase: 04-dashboard-shell-redesign
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/views/dashboard-shell/app-sidebar.tsx
  - packages/views/dashboard-shell/app-sidebar.test.tsx
  - packages/views/dashboard-shell/index.ts
  - packages/views/layout/index.ts
autonomous: true
requirements: [SHL-01, SHL-04]

must_haves:
  truths:
    - "AppSidebar renders Wordmark + WorkspaceSwitcher + nav groups (Personal/Workspace/Configure) + PriorityGrid + footer (Notifications/DarkMode/Collapse/Help)"
    - "AppSidebar accepts `wsId` as a prop AND passes it down to PriorityGrid + NotificationsBadge"
    - "Workspace-name fallback string is 'AlgoPlan' (NOT 'Multica')"
    - "AppSidebar still accepts `topSlot` prop for Desktop's existing back/forward SidebarTopBar (Pitfall 2 — dual topSlot pattern preserved)"
    - "AppSidebar's root may call useCurrentWorkspace() (allowed at sidebar root per UI-SPEC §2); sub-components do NOT call useWorkspaceId()"
    - "AppSidebar exports from packages/views/dashboard-shell/index.ts AND packages/views/layout/index.ts re-exports it from the new location (legacy app imports continue to work)"
    - "Active sidebar nav row gets a brand AccentBar (Phase 2 atom) as a leading flex child (UI-SPEC §2 + RESEARCH Pattern 5)"
  artifacts:
    - path: "packages/views/dashboard-shell/app-sidebar.tsx"
      provides: "Sidebar composition — replaces packages/views/layout/app-sidebar.tsx"
      min_lines: 200
    - path: "packages/views/dashboard-shell/app-sidebar.test.tsx"
      provides: "Composition + wsId-prop + AlgoPlan fallback + collapsed-state tests"
    - path: "packages/views/dashboard-shell/index.ts"
      provides: "Updated barrel — re-exports AppSidebar plus the five Plan 01 atoms"
    - path: "packages/views/layout/index.ts"
      provides: "Updated shim — `export { AppSidebar } from \"../dashboard-shell\";` (DashboardLayout still re-exported from layout/dashboard-layout until Plan 05 + 06 finish migration)"
  key_links:
    - from: "packages/views/dashboard-shell/app-sidebar.tsx"
      to: "packages/views/dashboard-shell/{wordmark,priority-grid,notifications-badge,dark-mode-toggle,collapse-toggle}"
      via: "named imports"
      pattern: "from \"\\./(wordmark|priority-grid|notifications-badge|dark-mode-toggle|collapse-toggle)\""
    - from: "packages/views/layout/index.ts"
      to: "packages/views/dashboard-shell/index.ts"
      via: "re-export"
      pattern: "export \\{ AppSidebar \\} from \"\\.\\./dashboard-shell\""
    - from: "packages/views/dashboard-shell/app-sidebar.tsx"
      to: "packages/ui/components/ui/sidebar (Sidebar, SidebarHeader, SidebarContent, SidebarFooter, SidebarGroup, SidebarMenu, SidebarRail)"
      via: "primitive composition"
      pattern: "from \"@multica/ui/components/ui/sidebar\""
---

<objective>
Compose the five Plan 01 atoms into the new `<AppSidebar>` at `packages/views/dashboard-shell/app-sidebar.tsx`. Port the existing logic from the legacy `packages/views/layout/app-sidebar.tsx` (681 lines) — preserve the existing WorkspaceSwitcher dropdown, Personal/Workspace/Configure nav groups, Pinned section, the global `C` keyboard shortcut, the `<HelpLauncher>` footer item, and the existing `topSlot` prop (Desktop's back/forward `SidebarTopBar` still mounts there per Pitfall 2). REPLACE the literal "Multica" workspace-name fallback with "AlgoPlan". ADD the PriorityGrid before the Configure group. ADD the NotificationsBadge / DarkModeToggle / CollapseToggle to the footer. Mutate `packages/views/dashboard-shell/index.ts` to export `AppSidebar` and the Plan 01 atoms; mutate `packages/views/layout/index.ts` to re-export `AppSidebar` from the new path so apps don't churn (Pitfall 7).

Purpose: This is the heart of the visual SHL-01 deliverable. After this plan ships, the new sidebar renders end-to-end except for `<DashboardShell>` (Plan 05) and the actual app wiring (Plan 06). The legacy `packages/views/layout/app-sidebar.tsx` stays in place until Plan 06 deletes it.

Output: One ~200–400 line composition file + one test file + two updated barrel files. No app-side files change yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-shell-redesign/04-UI-SPEC.md
@.planning/phases/04-dashboard-shell-redesign/04-RESEARCH.md
@.planning/phases/04-dashboard-shell-redesign/04-01-SUMMARY.md
@CLAUDE.md
@packages/views/layout/app-sidebar.tsx
@packages/views/layout/help-launcher.tsx
@packages/views/dashboard-shell/wordmark.tsx
@packages/views/dashboard-shell/priority-grid.tsx
@packages/views/dashboard-shell/notifications-badge.tsx
@packages/views/dashboard-shell/dark-mode-toggle.tsx
@packages/views/dashboard-shell/collapse-toggle.tsx
@packages/ui/components/ui/sidebar.tsx
@packages/ui/components/ui/accent-bar.tsx

<interfaces>
<!-- The legacy app-sidebar.tsx is 681 lines. DO NOT re-derive — port it. The new file should be a near-line-for-line port with substitutions. -->

Substitutions to apply when porting `packages/views/layout/app-sidebar.tsx` → `packages/views/dashboard-shell/app-sidebar.tsx`:

1. `"Multica"` literal in the workspace-name fallback (search for the workspace switcher trigger label) → use `WordmarkText` constant imported from `./wordmark` (= `"AlgoPlan"`).
2. The plain-text inbox unread `<span className="ml-auto text-xs">{count}</span>` → DELETE that block; instead render `<NotificationsBadge wsId={wsId} />` from `./notifications-badge` (which also wraps the SidebarMenuItem + SidebarMenuButton with the Inbox link).
3. ADD `<PriorityGrid wsId={wsId} />` inside SidebarContent, between the Workspace nav group and the Configure nav group. Wrap in a `<SidebarGroup>` with a `<SidebarGroupLabel>Priority</SidebarGroupLabel>`.
4. ADD a "Personal" SidebarGroupLabel above the existing personal nav items (Inbox / My Issues etc.) — UI-SPEC Copywriting Contract row "Personal".
5. REPLACE the workspace switcher trigger's literal label rendering with `<Wordmark workspaceName={currentWorkspace?.name}>` — the `<Wordmark>` from Plan 01 already renders the label + brand dot + chevron.
6. ADD to SidebarFooter the trio: `<NotificationsBadge>` is part of SidebarContent (it's the Inbox row, lives in Personal group); the FOOTER trio is `<DarkModeToggle />` + `<CollapseToggle />` + the existing `<HelpLauncher />`. Per UI-SPEC §2 footer composition.
7. ADD the brand AccentBar to active nav rows. Per UI-SPEC §2 + RESEARCH Pattern 5: use Phase 2 `<AccentBar color="brand" orientation="vertical" />` as a leading flex child of every `SidebarMenuButton`, gated by `data-active:opacity-100 opacity-0` (or render conditionally on `isActive`). Implementation should hide the bar via `opacity-0` when not active so layout doesn't shift on click.

WSId pass-through:
- `<AppSidebar>` accepts `{ wsId?: string; topSlot?: ReactNode; searchSlot?: ReactNode; headerClassName?: string; headerStyle?: CSSProperties }`.
- Inside `<AppSidebar>` the root MAY call `useCurrentWorkspace()` to derive any workspace context the OLD sidebar already used. Sub-components receive `wsId` from the prop (Pattern 2 from RESEARCH).
- If `wsId` prop is undefined, `<AppSidebar>` falls back to deriving from `useCurrentWorkspace()` (allowed at root only). This keeps backward compatibility with the existing call sites in `apps/web/.../layout.tsx` which currently pass no wsId; Plan 06 updates the apps to pass it explicitly.

WordmarkText reuse for dropdown header:
- The existing WorkspaceSwitcher dropdown menu (currently in `app-sidebar.tsx` lines ~423–531 per RESEARCH §Don't Hand-Roll row 4) opens via the Wordmark trigger. UI-SPEC §3 mandates a small "AlgoPlan" header at the TOP of the dropdown (`text-xs text-muted-foreground`). Add it as the first child of `<DropdownMenuContent>` using the imported `WordmarkText` constant.

From packages/ui/components/ui/accent-bar.tsx (Phase 2 atom — verified ships):
```typescript
export interface AccentBarProps {
  color: "brand" | "tag-p0" | "tag-p1" | "tag-p2" | "tag-p3" | "muted";
  orientation?: "vertical" | "horizontal";
  segments?: 1 | 2 | 3 | 4;
  className?: string;
}
export function AccentBar(props: AccentBarProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port app-sidebar.tsx into dashboard-shell/ + apply 7 substitutions + ship test</name>
  <files>
    packages/views/dashboard-shell/app-sidebar.tsx,
    packages/views/dashboard-shell/app-sidebar.test.tsx
  </files>
  <behavior>
    Tests:
    - Rendering with wsId="ws-1" mounts: `<Wordmark>`, the Personal nav group label "Personal", at least one nav button per group (Personal/Workspace/Configure), `<PriorityGrid>` (asserted by 4 P0/P1/P2/P3 buttons), `<NotificationsBadge>`, `<DarkModeToggle>`, `<CollapseToggle>`, `<HelpLauncher>` (or its identifying icon)
    - Rendering with wsId={undefined} does NOT throw — falls back to root-level useCurrentWorkspace(); when no workspace exists, the Wordmark trigger label shows literal "AlgoPlan" (the WordmarkText fallback)
    - Workspace-name fallback string in the trigger when no workspace name: "AlgoPlan" (search by `getByText("AlgoPlan")` in the trigger button); critical regression test for the Multica→AlgoPlan rename
    - Active nav row carries an AccentBar (color="brand", orientation="vertical") as a child element — assert by querying for the AccentBar's data-attribute or class signature
    - topSlot prop is rendered when provided (test passes `<div data-testid="custom-top">drag-strip</div>` and asserts presence) — preserves the dual-topSlot pattern from Pitfall 2
    - When `state="collapsed"` is forced on the SidebarProvider wrapper (or `data-collapsible="icon"` is set on the inner Sidebar root via test setup), the PriorityGrid container has the `group-data-[collapsible=icon]:hidden` class — sufficient assertion (real CSS hide behavior is owned by primitive)
    - Mocks (vi.hoisted + Object.assign per CLAUDE.md): `useCurrentWorkspace`, `useIssueViewStore`, `inboxKeys.list`, `api.listInbox`, `useNavigation`, `useTheme`, `useWorkspacePaths`. Wrap in `QueryClientProvider`.
  </behavior>
  <action>
    A. Read `packages/views/layout/app-sidebar.tsx` end-to-end (681 lines). Copy its entire content into `packages/views/dashboard-shell/app-sidebar.tsx`. Update relative imports:
    - Local imports of `./help-launcher` → `../layout/help-launcher` (HelpLauncher stays in layout/ per UI-SPEC).
    - Imports from `../navigation`, `../modals/registry`, etc. continue to work because the new file is one directory deeper at the same nesting.
    - Adjust any `import` statements whose relative path needs `../` prepended.

    B. Apply the SEVEN substitutions listed in `<interfaces>` above:
    1. Workspace-name fallback string: replace literal `"Multica"` with `WordmarkText` constant from `./wordmark` (and remove the inline string).
    2. Replace the inbox unread `<span className="ml-auto text-xs">` block with `<NotificationsBadge wsId={wsId} />`. Delete the duplicated `useQuery(inboxKeys.list…)` block from the body — the badge owns it now. Verify no other code in the file still references `unreadCount` after this; if so, delete those branches.
    3. Insert PriorityGrid between Workspace and Configure groups:
       ```jsx
       <SidebarGroup>
         <SidebarGroupLabel>Priority</SidebarGroupLabel>
         <PriorityGrid wsId={wsId} />
       </SidebarGroup>
       ```
    4. Add `<SidebarGroupLabel>Personal</SidebarGroupLabel>` above the existing personal nav items (Inbox / My Issues / etc. — locate by reading the existing file structure).
    5. Replace the workspace-switcher trigger label rendering with `<Wordmark workspaceName={currentWorkspace?.name} />`. Keep the surrounding `<DropdownMenuTrigger>` + `<SidebarMenuButton>` wrapper so `<Wordmark>`'s composition slots cleanly inside.
    6. SidebarFooter: keep the existing `<HelpLauncher>` and ADD `<DarkModeToggle />` + `<CollapseToggle />` siblings. Suggested layout: `<div className="flex items-center justify-between p-2">{leftSlot: helpLauncher}<div className="flex gap-1"><DarkModeToggle /><CollapseToggle /></div></div>` — adjust to match UI-SPEC §2 footer ordering (Notifications LEFT but the badge is part of Personal nav, not footer; the footer has DarkMode + Collapse on the RIGHT and HelpLauncher staying where it currently is).
    7. AccentBar on active rows: locate the central `SidebarMenuButton` rendering helper in the file (it's used in multiple places — Personal/Workspace/Configure groups). Wrap or inject `<AccentBar color="brand" orientation="vertical" className={cn("absolute inset-y-0 left-0 w-0.5", isActive ? "opacity-100" : "opacity-0")} />` as the first child of the SidebarMenuButton. Choose the leading flex child option (RESEARCH Pattern 5 recommendation).

    C. Top-level wsId resolution:
    - Add to props: `wsId?: string;`
    - Inside the component: `const currentWorkspace = useCurrentWorkspace(); const resolvedWsId = wsId ?? currentWorkspace?.id ?? undefined;`
    - Pass `resolvedWsId` to PriorityGrid + NotificationsBadge.

    D. WordmarkText in dropdown header:
    - Locate `<DropdownMenuContent>` of the workspace switcher dropdown. Add as the first child:
      ```jsx
      <div className="px-2 py-1.5 text-xs text-muted-foreground">{WordmarkText}</div>
      ```
    - This satisfies UI-SPEC §3 dropdown header.

    E. Mutate `packages/views/dashboard-shell/index.ts`:
    ```typescript
    export { AppSidebar } from "./app-sidebar";
    export { Wordmark, WordmarkText } from "./wordmark";
    export { PriorityGrid } from "./priority-grid";
    export { NotificationsBadge } from "./notifications-badge";
    export { DarkModeToggle } from "./dark-mode-toggle";
    export { CollapseToggle } from "./collapse-toggle";
    ```

    F. Mutate `packages/views/layout/index.ts`:
    ```typescript
    export { AppSidebar } from "../dashboard-shell";
    export { DashboardGuard } from "./dashboard-guard";
    export { DashboardLayout } from "./dashboard-layout";
    export { useDashboardGuard } from "./use-dashboard-guard";
    export { WorkspaceLoader } from "./workspace-loader";
    ```

    G. `app-sidebar.test.tsx`:
    - Mock all the @multica/core dependencies via vi.hoisted + Object.assign. Wrap in `QueryClientProvider`.
    - Six test cases per `<behavior>` block.
    - For the AccentBar assertion, use `container.querySelector('[data-accent-bar]')` (Phase 2 atom should expose a data attribute) or assert via class presence.

    DO NOT touch `packages/views/layout/app-sidebar.tsx` — it stays in place serving any callers that still bypass the barrel. Plan 06 deletes it.
    DO NOT touch `packages/views/layout/dashboard-layout.tsx` yet — Plan 05 replaces it; Plan 06 deletes it.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views exec vitest run dashboard-shell/app-sidebar.test.tsx && bash scripts/grep-no-useworkspaceid-in-shell.sh && pnpm typecheck && ! grep -rnE '#[0-9a-fA-F]{3,8}|rgb\(|dark:' packages/views/dashboard-shell/app-sidebar.tsx</automated>
  </verify>
  <done>
    - app-sidebar.tsx + test exist; tests pass; AlgoPlan fallback verified
    - Both barrel index.ts files updated and `pnpm typecheck` is green
    - grep hook still passes (root may use useCurrentWorkspace but NOT useWorkspaceId; sub-components receive wsId as prop)
    - AccentBar on active rows; PriorityGrid between Workspace and Configure; Personal label above personal nav; DarkMode + Collapse in footer
    - topSlot prop preserved (back/forward chrome on Desktop still works after Plan 06 wires both apps)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar root | May call `useCurrentWorkspace()` (allowed); sub-components must NOT |
| Workspace name fallback | User-visible string — wrong fallback ships brand inconsistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | AppSidebar wsId pass-through | mitigate | Test asserts `<AppSidebar wsId={undefined} />` does not throw + grep CI hook blocks any future `useWorkspaceId(` call. Sub-components receive resolved wsId from root. |
| T-04-02-02 | Tampering | "Multica" → "AlgoPlan" rename | mitigate | Test asserts `getByText("AlgoPlan")` in the trigger when no workspace selected; regression-locks the rename |
| T-04-02-03 | Spoofing | Workspace switcher dropdown header | accept | Header is purely cosmetic (`text-xs text-muted-foreground`) — no clickable target; just identity affirmation |
</threat_model>

<verification>
- `pnpm --filter @multica/views exec vitest run dashboard-shell/app-sidebar.test.tsx` passes
- `pnpm typecheck` is green (both apps + packages compile against the updated layout/ shim)
- `bash scripts/grep-no-useworkspaceid-in-shell.sh` exits 0
- No `dark:*`, no hex/RGB inside `app-sidebar.tsx`
- `packages/views/layout/index.ts` re-exports AppSidebar from the new path; existing app imports of `@multica/views/layout` still resolve
</verification>

<success_criteria>
- New AppSidebar mounts cleanly under jsdom with mocked stores
- All five Plan 01 atoms are composed in their UI-SPEC §2 positions
- Active nav row carries the brand AccentBar (Phase 2 atom reuse, NOT inline div)
- "AlgoPlan" string surfaces in three places: Wordmark trigger fallback, dropdown header, collapsed-mode tooltip
- Test count includes ≥6 cases covering composition, wsId-undefined safety, AlgoPlan fallback, AccentBar presence, topSlot pass-through, collapsed-state hide class
- No legacy file deletion yet
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-shell-redesign/04-02-SUMMARY.md` documenting:
- The seven substitutions applied during the port + their line locations in the new file
- AccentBar implementation choice (leading flex child) + rationale linking back to RESEARCH Pattern 5
- Confirmation `apps/web/.../layout.tsx` and `apps/desktop/.../desktop-layout.tsx` still resolve `AppSidebar` via the layout/ shim
- Reminder for Plan 06: legacy `packages/views/layout/app-sidebar.tsx` is now dead code — delete in Plan 06 alongside `dashboard-layout.tsx`
</output>
