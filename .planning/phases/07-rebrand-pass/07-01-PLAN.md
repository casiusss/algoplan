---
phase: 07-rebrand-pass
plan: 01
type: execute
wave: 1
depends_on: [07-00]
files_modified:
  - packages/views/chat/components/context-anchor.tsx
  - packages/views/chat/components/chat-fab.tsx
  - packages/views/chat/components/chat-window.tsx
  - packages/views/auth/login-page.tsx
  - packages/views/layout/help-launcher.tsx
  - packages/views/layout/workspace-loader.tsx
  - packages/views/workspace/create-workspace-form.tsx
  - packages/views/runtimes/components/runtime-list.tsx
  - packages/views/runtimes/components/update-section.tsx
  - packages/views/onboarding/utils/starter-content-templates.ts
  - packages/views/issues/components/issue-detail.tsx
  - packages/views/editor/utils/link-handler.ts
  - packages/views/search/search-command.test.tsx
  - packages/ui/styles/tokens.css
  - packages/ui/styles/base.css
  - packages/ui/markdown/file-cards.ts
  - packages/ui/markdown/Markdown.tsx
  - packages/core/paths/reserved-slugs.ts
autonomous: true
requirements: [RBR-01]
must_haves:
  truths:
    - "All user-visible Multica strings in packages/views are AlgoPlan (chat, auth, layout, workspace, runtimes, onboarding, issues)"
    - "All user-visible Multica strings in packages/ui are AlgoPlan (CSS comment headers; markdown CDN doc-comment hostname kept verbatim if multica-static.copilothub.ai is the production CDN)"
    - "packages/core reserved-slugs.ts blocks BOTH multica AND algoplan (anti-impersonation; users cannot create either workspace)"
    - "multica:* localStorage keys (multica:backlog-agent-hint-dismissed) are NOT renamed (data preservation)"
    - "@multica/* imports unchanged"
  artifacts:
    - path: "packages/views/chat/components/context-anchor.tsx"
      provides: "Chat context-anchor with AlgoPlan brand strings"
      contains: "AlgoPlan knows you're viewing"
    - path: "packages/views/chat/components/chat-fab.tsx"
      provides: "Chat FAB with AlgoPlan brand"
      contains: "Welcome to AlgoPlan"
    - path: "packages/core/paths/reserved-slugs.ts"
      provides: "Reserved slugs include both multica and algoplan"
      contains: "\"algoplan\""
  key_links:
    - from: "scripts/grep-rebrand.sh"
      to: "packages/views/* and packages/ui/* and packages/core/*"
      via: "exclusion regex must match localStorage multica: prefix"
      pattern: "multica:backlog|multica:chat"
---

<objective>
Wave 1 — sweep user-visible "Multica" strings out of the three shared packages (`packages/views`, `packages/ui`, `packages/core`). This is the largest single batch (~96 occurrences across 18 files) and lands in one plan to keep all shared-package edits in a single reviewable diff.

Purpose: Shared packages are consumed by both apps. Splitting this work across plans creates merge conflicts on the same files (e.g. `chat-window.tsx` referenced by web + desktop). One plan, one diff, one review.
Output: 18 files edited; the only "Multica" strings remaining in `packages/*` after this plan are inside the explicit exclusion patterns (verified by `bash scripts/grep-rebrand.sh` scoped to packages/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-rebrand-pass/07-CONTEXT.md
@.planning/phases/07-rebrand-pass/07-PATTERNS.md
@.planning/phases/07-rebrand-pass/07-VALIDATION.md
@scripts/grep-rebrand.sh
@packages/views/auth/algoplan-wordmark.tsx
@packages/ui/components/common/multica-icon.tsx
</context>

<interfaces>
<!-- Existing brand atoms (DO NOT modify in this plan; consume only) -->

From packages/views/auth/algoplan-wordmark.tsx:
```typescript
export function AlgoPlanWordmark(props: {
  size?: "default" | "lg";
  className?: string;
}): JSX.Element;
```

From packages/ui/components/common/multica-icon.tsx (KEPT VERBATIM — aesthetic asterisk):
```typescript
export function MulticaIcon(props: {
  className?: string;
  noSpin?: boolean;
} & React.ComponentProps<"span">): JSX.Element;
```

Decision rule for swapping `<MulticaIcon/>` → `<AlgoPlanWordmark/>`:
- Use AlgoPlanWordmark when the rendering is BRAND IDENTITY (sidebar wordmark, auth pages, full-app loader) — the user perceives it as the app name
- Keep MulticaIcon when the rendering is DECORATIVE (small inline asterisk in tooltip, list-item bullet, status indicator) — the user perceives it as a UI ornament

Per Plan 07-PATTERNS §1, the only swap site in this plan is:
- `packages/views/layout/workspace-loader.tsx:23` → use AlgoPlanWordmark size="lg" (full-screen workspace-loading splash IS brand identity)
- `packages/views/onboarding/steps/step-welcome.tsx:80` → KEEP MulticaIcon (size-5 inline asterisk inside a step card; decorative)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sweep packages/views — chat, auth, layout, workspace, runtimes, onboarding, issues, editor, search</name>
  <files>packages/views/chat/components/context-anchor.tsx, packages/views/chat/components/chat-fab.tsx, packages/views/chat/components/chat-window.tsx, packages/views/auth/login-page.tsx, packages/views/layout/help-launcher.tsx, packages/views/layout/workspace-loader.tsx, packages/views/workspace/create-workspace-form.tsx, packages/views/runtimes/components/runtime-list.tsx, packages/views/runtimes/components/update-section.tsx, packages/views/onboarding/utils/starter-content-templates.ts, packages/views/issues/components/issue-detail.tsx, packages/views/editor/utils/link-handler.ts, packages/views/search/search-command.test.tsx</files>
  <action>
  Apply the §1 Replacement Table from `.planning/phases/07-rebrand-pass/07-PATTERNS.md` for every row scoped to `packages/views/*`. Per file:

  **`packages/views/chat/components/context-anchor.tsx`** (6 refs, lines 131, 134, 135, 136, 149, 182, 183) — replace "Multica" with "AlgoPlan" in all visible message strings.

  **`packages/views/chat/components/chat-fab.tsx`** (lines 37, 40) — "Multica is working..." → "AlgoPlan is working...", "Ask Multica" → "Ask AlgoPlan".

  **`packages/views/chat/components/chat-window.tsx`** (line 629) — "Welcome to Multica" → "Welcome to AlgoPlan".

  **`packages/views/auth/login-page.tsx`** (line 64) — comment-only: "legacy MulticaIcon during transitional" → KEEP (developer comment explaining historical migration; not user-visible). Skip this file unless other refs present (re-grep to confirm).

  **`packages/views/layout/help-launcher.tsx`** (lines 12, 13) — `https://multica.ai/docs` → `https://algoplan.ai/docs`, `https://multica.ai/changelog` → `https://algoplan.ai/changelog`. Per planner Q4 follow-up: domain swap requires DNS readiness (flagged in SUMMARY); if `algoplan.ai` is not yet owned, executor MUST checkpoint before applying this file. Otherwise apply.

  **`packages/views/layout/workspace-loader.tsx`** (line 3 import + line 23 JSX): swap `<MulticaIcon className="size-8 animate-pulse"/>` → `<AlgoPlanWordmark size="lg" className="animate-pulse"/>`. Update import: `import { MulticaIcon } from "@multica/ui/components/common/multica-icon";` → `import { AlgoPlanWordmark } from "@multica/views/auth/algoplan-wordmark";` (note: this creates a layout→auth dependency; if circular, lift `AlgoPlanWordmark` to `packages/views/branding/algoplan-wordmark.tsx` in a 3-line subtask). Verify no test on workspace-loader breaks (`pnpm --filter @multica/views exec vitest run layout/workspace-loader`).

  **`packages/views/workspace/create-workspace-form.tsx`** (line 89) — `multica.ai/` placeholder string → `algoplan.ai/`.

  **`packages/views/runtimes/components/runtime-list.tsx`** (line 234) — `multica daemon start` → KEEP (CLI command; planner Q1 says CLI binary stays `multica`). Confirm via re-grep that this is the only ref in this file; skip the file otherwise.

  **`packages/views/runtimes/components/update-section.tsx`** (line 180) — title attribute "...managed by Multica Desktop — update Desktop to upgrade the CLI." → "...managed by AlgoPlan Desktop — update Desktop to upgrade the CLI."

  **`packages/views/onboarding/utils/starter-content-templates.ts`** (lines 41, 53) — "Welcome to Multica! 👋" → "Welcome to AlgoPlan! 👋"; "**Explain how we work together in Multica**" → "**Explain how we work together in AlgoPlan**".

  **`packages/views/issues/components/issue-detail.tsx`** (lines 475, 983) — these are `localStorage.getItem("multica:backlog-agent-hint-dismissed")` and `localStorage.setItem("multica:backlog-agent-hint-dismissed", "true")`. PRESERVE VERBATIM (D-2 extension — `multica:*` localStorage keys = data loss risk). Confirm by reading the lines and skipping the file.

  **`packages/views/editor/utils/link-handler.ts`** (lines 34, 55) — `multica:navigate` custom DOM event → PRESERVE VERBATIM (internal pub/sub event; renaming would require coordinated change with `apps/desktop/src/renderer/src/components/desktop-layout.tsx:98-99` which is also `multica:navigate`. Per planner decision, custom event names are app-internal and out of scope). Skip this file.

  **`packages/views/search/search-command.test.tsx`** (lines 32, 145, 302) — test fixture URLs use `https://app.multica/...`. Update to `https://app.algoplan/...` (test-only; no production effect).

  **`packages/views/modals/create-issue.tsx`** (lines 100, 174 — referenced in grep but NOT in files_modified) — these are `multica:backlog-agent-hint-dismissed` localStorage keys. PRESERVE. Re-grep after edit confirms zero remaining user-visible refs in this file.

  After all edits, run `pnpm --filter @multica/views test` — all existing tests must pass. Some test assertions may break (search-command.test.tsx now asserts `https://app.algoplan/...`); fix those test assertions in the same edit pass.
  </action>
  <verify>
    <automated>pnpm --filter @multica/views test 2>&1 | tail -10 && grep -rnE "[Mm]ultica" packages/views --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v node_modules | grep -v "\.turbo" | grep -vE "(@multica/|multica_|multica:backlog|multica:chat|multica:navigate|MulticaIcon|MULTICA_|server/cmd/multica|multica daemon)" | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
  Zero user-visible "Multica" hits remain in `packages/views/*` after applying the exclusion regex. All Vitest tests in `@multica/views` pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep packages/ui — CSS comment headers + markdown CDN doc-comments</name>
  <files>packages/ui/styles/tokens.css, packages/ui/styles/base.css, packages/ui/markdown/file-cards.ts, packages/ui/markdown/Markdown.tsx</files>
  <action>
  **`packages/ui/styles/tokens.css`** line 1: `/* Multica design tokens — shared across Web + Desktop */` → `/* AlgoPlan design tokens — shared across Web + Desktop */`

  **`packages/ui/styles/base.css`** lines 2 + 25:
  - line 2: ` * Multica shared base styles — imported by all apps` → ` * AlgoPlan shared base styles — imported by all apps`
  - line 25: `/* Multica icon: entrance spin animation */` → `/* AlgoPlan icon: entrance spin animation */`. Note: the keyframes name itself (e.g. `multica-icon-spin`) — read the file first; if the keyframes identifier is `multica-*`, it is referenced by `MulticaIcon` component which we're keeping. Preserve the keyframes identifier verbatim; only edit the comment header.

  **`packages/ui/markdown/file-cards.ts`** + **`packages/ui/markdown/Markdown.tsx`** — these reference `multica-static.copilothub.ai` in JSDoc as the example CDN hostname. This is an EXAMPLE STRING in documentation, not a hardcoded production hostname (the actual hostname is passed as `cdnDomain` prop). Per planner discretion: update example to `algoplan-static.copilothub.ai` (cosmetic; no runtime effect). If grep-rebrand.sh re-add this as a hit, it's accepted — the script regex is for production strings, not JSDoc examples; relax the script in plan 07-00 if needed.

  **`packages/ui/components/common/multica-icon.tsx`** — KEEP. Per VALIDATION.md: aesthetic asterisk component, file rename deferred (touches 5+ files for zero user impact). Verify `bash scripts/grep-rebrand.sh` excludes it.
  </action>
  <verify>
    <automated>pnpm --filter @multica/ui test 2>&1 | tail -10 && grep -rnE "[Mm]ultica" packages/ui --include="*.tsx" --include="*.ts" --include="*.css" 2>/dev/null | grep -v node_modules | grep -v "\.turbo" | grep -vE "(@multica/|multica_|multica:|MulticaIcon|multica-icon|multica-icon-spin|MULTICA_)" | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
  Zero user-visible "Multica" in packages/ui after exclusions. CSS comment headers say AlgoPlan. Markdown JSDoc example CDN updated. MulticaIcon component file unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update packages/core reserved-slugs.ts — block both names</name>
  <files>packages/core/paths/reserved-slugs.ts</files>
  <action>
  Read `packages/core/paths/reserved-slugs.ts`. Find the `"multica"` entry (line 31, comment: `// brand name — prevent impersonation workspaces`).

  Add `"algoplan"` to the array (place it adjacent to `"multica"`, alphabetically). Update the comment on the `"multica"` entry to: `// legacy brand name — prevent impersonation workspaces (kept post-rebrand to block /multica slug)`. Add a comment on the new `"algoplan"` entry: `// brand name — prevent impersonation workspaces`.

  Do NOT remove `"multica"`. The slug must remain reserved forever (anti-impersonation post-rebrand).

  After editing, run `pnpm --filter @multica/core test` — there is likely a unit test asserting reserved slug behavior; if so, extend it to assert both `"multica"` AND `"algoplan"` are blocked.

  Other `multica` refs in `packages/core/*` are ALL `multica:chat:*` localStorage keys (lines in `chat/store.ts`, `platform/storage-cleanup.ts`, `platform/storage-cleanup.test.ts`) — PRESERVE VERBATIM (D-2 extension). Verify by re-grep.
  </action>
  <verify>
    <automated>pnpm --filter @multica/core test 2>&1 | tail -5 && grep -E "\"(algoplan|multica)\"" packages/core/paths/reserved-slugs.ts | wc -l | awk '{exit ($1 == 2) ? 0 : 1}' && grep -rnE "[Mm]ultica" packages/core --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v node_modules | grep -v "\.turbo" | grep -vE "(@multica/|multica_|multica:chat|multica:navigate|MULTICA_|reserved-slugs)" | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
  reserved-slugs.ts contains both "multica" and "algoplan". All `multica:chat:*` localStorage keys are unchanged. `pnpm --filter @multica/core test` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-input → workspace slug | User-supplied workspace slug must not impersonate the brand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Spoofing | reserved-slugs.ts | mitigate | Block BOTH "multica" AND "algoplan" — prevents legacy URL impersonation post-rebrand |
| T-07-01-02 | Information Disclosure | localStorage | mitigate | Verify-by-grep that no `multica_*` or `multica:*` localStorage key was renamed during the sweep (data loss prevention is a confidentiality issue: user theme/draft loss = silent data destruction) |
</threat_model>

<verification>
```bash
# Combined Wave 1 audit (packages/* only)
bash scripts/grep-rebrand.sh 2>&1 | grep -E "^packages/" || echo "✓ packages/* clean"
# Cross-check storage keys preserved
grep -rn "multica_token\|multica:chat\|multica:backlog\|multica_theme" packages --include="*.ts" --include="*.tsx" | grep -v node_modules | wc -l
# Expected: > 0 (keys still present)
```
</verification>

<success_criteria>
- `bash scripts/grep-rebrand.sh` shows zero hits originating from `packages/views` `packages/ui` `packages/core`
- All shared-package Vitest suites green: `pnpm --filter @multica/views test`, `pnpm --filter @multica/ui test`, `pnpm --filter @multica/core test`
- `multica_*` and `multica:*` localStorage keys are verifiably unchanged (grep returns expected count)
- `MulticaIcon` component file is untouched (only ONE swap site in workspace-loader.tsx; onboarding step keeps it)
- `multica:navigate` custom DOM event is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebrand-pass/07-01-SUMMARY.md` per template.
Track in SUMMARY:
  - exact list of files edited (subset of files_modified)
  - any rows from 07-PATTERNS §1 that were SKIPPED (with rationale referencing exclusion rule)
  - whether Task 1 had to lift AlgoPlanWordmark out of `auth/` due to circular import (and the new path)
  - DNS readiness checkpoint outcome (help-launcher.tsx domain swap)
</output>
