---
phase: 07-rebrand-pass
plan: 02
type: execute
wave: 1
depends_on: [07-00]
files_modified:
  - apps/web/app/layout.tsx
  - apps/web/app/robots.ts
  - apps/web/app/sitemap.ts
  - apps/web/app/(landing)/page.tsx
  - apps/web/app/(landing)/layout.tsx
  - apps/web/app/(landing)/homepage/page.tsx
  - apps/web/app/(landing)/about/page.tsx
  - apps/web/app/(landing)/changelog/page.tsx
  - apps/web/app/(landing)/download/page.tsx
  - apps/web/app/(landing)/download/download-client.tsx
  - apps/web/app/(auth)/workspaces/new/page.tsx
  - apps/web/app/(auth)/invite/[id]/page.tsx
  - apps/web/app/(auth)/onboarding/page.tsx
  - apps/web/app/[workspaceSlug]/layout.tsx
  - apps/web/app/[workspaceSlug]/(dashboard)/layout.tsx
  - apps/web/app/not-found.tsx
  - apps/web/app/custom.css
  - apps/web/features/landing/i18n/en.ts
  - apps/web/features/landing/i18n/zh.ts
  - apps/web/features/landing/i18n/context.tsx
  - apps/web/features/landing/components/multica-landing.tsx
  - apps/web/features/landing/components/landing-header.tsx
  - apps/web/features/landing/components/landing-footer.tsx
  - apps/web/features/landing/components/landing-hero.tsx
  - apps/web/features/landing/components/faq-section.tsx
  - apps/web/features/landing/components/features-section.tsx
  - apps/web/features/landing/components/how-it-works-section.tsx
  - apps/web/features/landing/components/redirect-if-authenticated.tsx
  - apps/web/features/landing/components/shared.tsx
  - apps/web/features/landing/components/download/hero.tsx
  - apps/web/features/landing/components/download/all-platforms.tsx
  - apps/web/features/landing/components/download/cli-section.tsx
  - apps/web/features/landing/components/download/cloud-section.tsx
  - apps/web/features/auth/auth-cookie.ts
  - apps/web/test/helpers.tsx
autonomous: false
requirements: [RBR-01, RBR-04]
must_haves:
  truths:
    - "All user-visible 'Multica' strings in apps/web are 'AlgoPlan' (root layout, landing, auth flow, dashboard, 404)"
    - "Landing i18n (English + Chinese) updated with AlgoPlan brand"
    - "All metadata.title / openGraph.siteName / twitter handle entries reflect AlgoPlan"
    - "Test helper email fixtures preserved or updated consistently (test@multica.ai → test@algoplan.ai)"
  artifacts:
    - path: "apps/web/app/layout.tsx"
      provides: "Root metadata with AlgoPlan brand"
      contains: "AlgoPlan — Project Management"
    - path: "apps/web/features/landing/i18n/en.ts"
      provides: "English landing copy with AlgoPlan brand"
      contains: "AlgoPlan"
    - path: "apps/web/features/landing/i18n/zh.ts"
      provides: "Chinese landing copy with AlgoPlan brand (Latin transliteration)"
      contains: "AlgoPlan"
  key_links:
    - from: "apps/web/app/layout.tsx metadataBase"
      to: "DNS algoplan.ai"
      via: "openGraph.url + canonical alternates"
      pattern: "https://www.algoplan.ai"
---

<objective>
Wave 1 — sweep the web app: root layout metadata, marketing landing site, auth flow pages, dashboard layout, error pages. Largest external-facing surface (~110 user-visible refs across 33 files), heavy concentration in `features/landing/i18n/{en,zh}.ts` (28 refs each).

Purpose: Web app metadata drives SEO, social previews, and browser tab labels — these are the most visible brand surfaces. Landing i18n drives the marketing site copy. Both must update atomically with `apps/desktop` strings (Plan 07-03) so the web→desktop user journey doesn't show mixed branding.
Output: 33 files edited; web `pnpm test` green; manual visual confirmation of landing site + tab title checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-rebrand-pass/07-CONTEXT.md
@.planning/phases/07-rebrand-pass/07-PATTERNS.md
@.planning/phases/07-rebrand-pass/07-VALIDATION.md
@scripts/grep-rebrand.sh
@apps/web/app/layout.tsx
</context>

<interfaces>
<!-- Existing landing i18n contract -->

apps/web/features/landing/i18n/{en,zh}.ts both export the same keyed object:
```typescript
export const en: LandingI18n = {
  hero: { title: ..., subtitle: ..., imageAlt: ... },
  features: { ... },
  faq: { items: [{ question, answer }, ...] },
  about: { title, subtitle, ... },
  changelog: { title, subtitle },
  download: { mac: { title, ... }, windows: { ... }, linux: { ... } },
  // ...
}
```

Both files MUST stay structurally identical (same keys). Preserve unicode-escape sequences in zh.ts (e.g. `关于 Multica` → `关于 AlgoPlan`). Chinese transliteration of "Multica" was already Latin "Multica" — replace with Latin "AlgoPlan" (no Chinese transliteration needed).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Edit apps/web/app/* — root layout metadata, route page metadata, error pages, custom CSS</name>
  <files>apps/web/app/layout.tsx, apps/web/app/robots.ts, apps/web/app/sitemap.ts, apps/web/app/not-found.tsx, apps/web/app/custom.css, apps/web/app/(landing)/page.tsx, apps/web/app/(landing)/layout.tsx, apps/web/app/(landing)/homepage/page.tsx, apps/web/app/(landing)/about/page.tsx, apps/web/app/(landing)/changelog/page.tsx, apps/web/app/(landing)/download/page.tsx, apps/web/app/(landing)/download/download-client.tsx, apps/web/app/(auth)/workspaces/new/page.tsx, apps/web/app/(auth)/invite/[id]/page.tsx, apps/web/app/(auth)/onboarding/page.tsx, apps/web/app/[workspaceSlug]/layout.tsx, apps/web/app/[workspaceSlug]/(dashboard)/layout.tsx</files>
  <action>
  Apply 07-PATTERNS §1 rows scoped to `apps/web/app/*`:

  **`apps/web/app/layout.tsx`** (lines 71, 72-75, 77, 84, 88, 89):
  - `metadataBase: new URL("https://www.multica.ai")` → `new URL("https://www.algoplan.ai")` (DNS — checkpoint Task 4)
  - `title.default: "Multica — Project Management for Human + Agent Teams"` → `"AlgoPlan — Project Management for Human + Agent Teams"`
  - `title.template: "%s | Multica"` → `"%s | AlgoPlan"`
  - `openGraph.siteName: "Multica"` → `"AlgoPlan"`
  - `twitter.site: "@multica_hq"` → `"@algoplan_hq"` (placeholder — confirm at checkpoint)
  - `twitter.creator: "@multica_hq"` → `"@algoplan_hq"`

  **`apps/web/app/robots.ts`** + **`apps/web/app/sitemap.ts`** — `baseUrl = "https://www.multica.ai"` → `"https://www.algoplan.ai"`. Confirm DNS at checkpoint.

  **`apps/web/app/not-found.tsx`** — any "Multica" string → "AlgoPlan".

  **`apps/web/app/custom.css`** — re-grep first; if comment-only, edit comments. If any custom property names contain `multica` they likely ARE consumed (e.g. `--multica-*` would be in selectors); preserve those and edit only string-literal comments.

  **`apps/web/app/(landing)/*.tsx`** — for each landing route:
  - `metadata.title` containing "Multica" → "AlgoPlan"
  - `metadata.description` containing "Multica" → "AlgoPlan"
  - `metadata.openGraph.url` containing `https://www.multica.ai` → `https://www.algoplan.ai`
  - Any in-component JSX text containing "Multica" → "AlgoPlan"

  **`apps/web/app/(auth)/workspaces/new/page.tsx`**, **`apps/web/app/(auth)/invite/[id]/page.tsx`**, **`apps/web/app/(auth)/onboarding/page.tsx`** — these are mostly `@multica/*` imports (preserve). Re-grep per file; if any user-visible string remains, replace.

  **`apps/web/app/[workspaceSlug]/layout.tsx`** (line 11 + 66): `<MulticaIcon className="size-6 animate-pulse" />` is a workspace-loading splash — per Plan 07-01 §workspace-loader.tsx decision, swap to `<AlgoPlanWordmark size="default" className="animate-pulse"/>`. Update import.

  **`apps/web/app/[workspaceSlug]/(dashboard)/layout.tsx`** — re-grep; update any user-visible strings.

  All other `apps/web/app/[workspaceSlug]/(dashboard)/*/page.tsx` files referenced in the directory grep (settings, inbox, projects, etc.) contain ONLY `@multica/*` imports; verify by re-grep and skip if no user-visible refs remain.
  </action>
  <verify>
    <automated>grep -rnE "[Mm]ultica" apps/web/app --include="*.tsx" --include="*.ts" --include="*.css" 2>/dev/null | grep -v node_modules | grep -v "\.next" | grep -vE "(@multica/|multica_|multica:|MULTICA_|MulticaIcon|multica-icon|multica://)" | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
  Zero user-visible "Multica" hits in `apps/web/app/*` after exclusions. All metadata reads "AlgoPlan". MulticaIcon swapped to AlgoPlanWordmark in workspace layout. Note: `multica://` deep-link strings in `auth/callback/page.tsx` are deliberately UNTOUCHED in this plan — Plan 07-04 owns the atomic flip.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep apps/web/features/landing — i18n (en/zh) + components + auth helpers + test fixtures</name>
  <files>apps/web/features/landing/i18n/en.ts, apps/web/features/landing/i18n/zh.ts, apps/web/features/landing/i18n/context.tsx, apps/web/features/landing/components/multica-landing.tsx, apps/web/features/landing/components/landing-header.tsx, apps/web/features/landing/components/landing-footer.tsx, apps/web/features/landing/components/landing-hero.tsx, apps/web/features/landing/components/faq-section.tsx, apps/web/features/landing/components/features-section.tsx, apps/web/features/landing/components/how-it-works-section.tsx, apps/web/features/landing/components/redirect-if-authenticated.tsx, apps/web/features/landing/components/shared.tsx, apps/web/features/landing/components/download/hero.tsx, apps/web/features/landing/components/download/all-platforms.tsx, apps/web/features/landing/components/download/cli-section.tsx, apps/web/features/landing/components/download/cloud-section.tsx, apps/web/features/auth/auth-cookie.ts, apps/web/test/helpers.tsx</files>
  <action>
  Bulk replace pass scoped to `apps/web/features/*`:

  **`apps/web/features/landing/i18n/en.ts`** (28 refs) — replace every `"Multica"` Latin occurrence with `"AlgoPlan"`. Preserve sentence structure exactly. Watch for compound phrases:
  - "Multica is" → "AlgoPlan is"
  - "Multica detects" → "AlgoPlan detects"
  - "Multica board view" → "AlgoPlan board view"
  - "Multica for macOS / Windows / Linux" → "AlgoPlan for macOS / Windows / Linux"
  - "About Multica" → "About AlgoPlan"
  - "What coding agents does Multica support?" → "What coding agents does AlgoPlan support?"
  - Any URL `https://multica.ai/*` → `https://algoplan.ai/*` (DNS — already gated by Task 4 checkpoint)
  - Email `support@multica.ai` (if present in copy) → `support@algoplan.ai`

  **`apps/web/features/landing/i18n/zh.ts`** (28 refs) — mirror en.ts edit-by-edit. Watch for unicode-escaped sequences:
  - `关于 Multica` ("关于 Multica") → `关于 AlgoPlan` ("关于 AlgoPlan")
  - `Multica 的...` → `AlgoPlan 的...`
  - "Multica for macOS" entries: keep Latin "Multica" → Latin "AlgoPlan" (no Chinese transliteration; the Latin name is the brand).

  Both en.ts and zh.ts MUST end with the same number of "AlgoPlan" occurrences as the pre-edit "Multica" count (28 each).

  **`apps/web/features/landing/i18n/context.tsx`** — re-grep; likely a Multica-named context constant. Rename to AlgoPlan if user-visible (probably internal — keep).

  **`apps/web/features/landing/components/multica-landing.tsx`** — file CONTENT update only, NOT a file rename (file rename touches imports and is out of scope per "preserve internal naming where possible"). Replace any "Multica" in JSX/strings → "AlgoPlan". Filename `multica-landing.tsx` stays.

  **All other landing component files** — re-grep first to identify user-visible refs. Replace each "Multica" → "AlgoPlan" in JSX text, alt text, aria-label, title attributes.

  **`apps/web/features/auth/auth-cookie.ts`** — re-grep; this is referenced in the directory count. Likely a `multica_token` cookie name → PRESERVE (D-2 — silent data loss if renamed; users get logged out across all sessions). Confirm by reading the file; preserve `multica_*` cookie names verbatim. Edit only user-visible string literals (if any).

  **`apps/web/test/helpers.tsx`** (lines 10, 44) — test fixture user emails `test@multica.ai` → `test@algoplan.ai`. Test-only; no production effect. Update consistently across all related tests.
  </action>
  <verify>
    <automated>pnpm --filter @multica/web test 2>&1 | tail -10 && grep -rnE "[Mm]ultica" apps/web/features apps/web/test --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v node_modules | grep -v "\.next" | grep -vE "(@multica/|multica_|multica:|MULTICA_|MulticaIcon|multica-landing\.tsx)" | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
  </verify>
  <done>
  Zero user-visible "Multica" in `apps/web/features/*` and `apps/web/test/*`. en.ts and zh.ts both contain 28 "AlgoPlan" occurrences (same count as pre-edit Multica refs). `multica_*` cookie names preserved. `pnpm --filter @multica/web test` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — browser tab + landing page + OG preview</name>
  <what-built>Web app with AlgoPlan brand strings throughout (Tasks 1+2). Landing site rebranded EN+ZH. Root metadata + OG metadata flipped.</what-built>
  <how-to-verify>
  Run `pnpm dev:web` (Claude restarts/starts the frontend per user global rule). Open http://localhost:3000 in browser:

  1. **Browser tab title** — should read "AlgoPlan — Project Management for Human + Agent Teams" (NOT Multica).
  2. **Landing page** — scroll through hero, features, FAQ, footer. Every visible brand mention says "AlgoPlan".
  3. **Toggle language EN ↔ ZH** (if landing has language toggle) — Chinese page also shows "AlgoPlan".
  4. **About page** (`/about`) — title "About AlgoPlan".
  5. **Download page** (`/download`) — "Download AlgoPlan", "AlgoPlan for macOS / Windows / Linux".
  6. **404 page** (`/some-nonexistent-route`) — any brand mention says AlgoPlan.
  7. **OG preview** — open browser DevTools → Elements → `<head>`. Confirm:
     - `<title>AlgoPlan — ...</title>`
     - `<meta property="og:site_name" content="AlgoPlan">`
     - `<meta property="og:url" content="https://www.algoplan.ai/">` (assuming DNS confirmed in Task 4)
     - `<meta name="twitter:site" content="@algoplan_hq">` (placeholder OK pending Twitter handle confirmation)
  8. **Workspace loading splash** — log in, switch workspaces. Splash shows AlgoPlanWordmark, NOT MulticaIcon asterisk.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "tab title still says Multica on /changelog")</resume-signal>
  <files>(checkpoint — no file mutation; verification only)</files>
  <action>(checkpoint task — Claude restarts the web dev server per user global rule, then user performs the 8-step browser verification listed in <how-to-verify>. Claude waits for 'approved' before proceeding to Task 4.)</action>
  <verify>
    <automated>pnpm --filter @multica/web typecheck 2>&1 | tail -5 && curl -sI http://localhost:3000 | head -1 || echo 'dev server not yet running — user starts manually'</automated>
  </verify>
  <done>User has visually confirmed all 8 verification points (tab title, landing brand, EN/ZH parity, about, download, 404, OG meta, splash) — or has reported issues that Claude has fixed and re-presented.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: DNS readiness decision — apply algoplan.ai or stay on multica.ai</name>
  <decision>Whether to flip every URL string `https://www.multica.ai` → `https://www.algoplan.ai` in the metadata, sitemap, robots, and landing copy as part of this plan, OR keep `multica.ai` until the algoplan.ai domain DNS is provisioned and verify that flip in a follow-up plan.</decision>
  <context>
  Tasks 1+2 in this plan replaced `https://www.multica.ai` with `https://www.algoplan.ai` in ~12 string sites (root metadata.metadataBase, sitemap baseUrl, robots baseUrl, landing OG urls, support email, FAQ links). If `algoplan.ai` is NOT yet owned + DNS-resolvable, this change ships:
    - 404s on every social-share preview link (LinkedIn, Twitter, Slack unfurls)
    - Broken canonical URLs (SEO regression — search engines de-index)
    - Broken `mailto:support@algoplan.ai` (email bounces)
  If `algoplan.ai` IS owned and DNS is live, the flip is safe to merge.
  </context>
  <options>
    <option id="option-a">
      <name>Apply algoplan.ai flip now</name>
      <pros>Single atomic rebrand commit; no follow-up plan needed; consistent with the principle "Phase 7 = full rebrand pass"</pros>
      <cons>Requires DNS + email infra ready BEFORE merge; otherwise production breaks</cons>
    </option>
    <option id="option-b">
      <name>Revert URL/email changes; keep multica.ai for now</name>
      <pros>Safe — production keeps working; Phase 7 ships strings + assets without depending on infra timeline</pros>
      <cons>Branding inconsistency — page title says "AlgoPlan" but OG URL says "multica.ai"; requires a Phase 7.1 follow-up plan to flip URLs once DNS is live; risks the "we'll do it later" trap</cons>
    </option>
    <option id="option-c">
      <name>Apply algoplan.ai for canonical/OG URLs ONLY; keep `support@multica.ai` email pending mail-server provisioning</name>
      <pros>Hybrid — captures the high-value SEO/social benefits as soon as DNS is live; defers email until mail server is provisioned (often a separate task)</pros>
      <cons>Requires the executor to discriminate per-string (DNS-only, not email); auditable but error-prone</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (algoplan.ai DNS confirmed live), option-b (keep multica.ai), option-c (URLs yes, email no), or describe alternative</resume-signal>
  <files>(checkpoint — no file mutation here; user decision routes Tasks 1+2 URL edits)</files>
  <action>(checkpoint task — Claude presents the three options listed in <options> with the consequences from <context>. User selects option-a/b/c. If option-b, Claude reverts the algoplan.ai URL edits applied in Tasks 1+2. If option-c, Claude reverts only support@algoplan.ai email edits, keeping URL flips. Final state recorded in plan SUMMARY.)</action>
  <verify>
    <automated>echo "decision recorded in 07-02-SUMMARY.md"</automated>
  </verify>
  <done>User has selected option-a, option-b, or option-c. Plan SUMMARY records the decision plus any reverts applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-input → app metadata | Search engines + social platforms scrape metadata; misconfigured canonical URL = SEO loss |
| user-input → cookies | `multica_token` auth cookie name MUST stay verbatim or all sessions are invalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | apps/web/features/auth/auth-cookie.ts | mitigate | Verify-by-grep that `multica_token` cookie name is NOT renamed during the sweep — Task 2 explicitly preserves D-2 keys |
| T-07-02-02 | Information Disclosure | metadata.metadataBase + canonical URLs | mitigate | DNS readiness checkpoint (Task 4) prevents shipping broken canonical URLs that would de-index the site |
| T-07-02-03 | Spoofing | landing OG metadata | accept | OG URL changing is expected; old shares revalidate within 24-48h; acceptable transitional cost |
</threat_model>

<verification>
```bash
# Web app full sweep audit
bash scripts/grep-rebrand.sh 2>&1 | grep -E "^apps/web/" || echo "✓ apps/web/* clean"
# Cookie name preservation
grep -rn "multica_token" apps/web --include="*.ts" --include="*.tsx" | grep -v node_modules | wc -l | xargs -I{} test {} -gt 0 && echo "✓ multica_token preserved"
# en/zh i18n parity
en_count=$(grep -c "AlgoPlan" apps/web/features/landing/i18n/en.ts)
zh_count=$(grep -c "AlgoPlan" apps/web/features/landing/i18n/zh.ts)
test "$en_count" -eq "$zh_count" && echo "✓ i18n parity ($en_count refs each)"
```
</verification>

<success_criteria>
- `bash scripts/grep-rebrand.sh` shows zero hits originating from `apps/web/*` (excluding `multica://` strings in `auth/callback/page.tsx` which Plan 07-04 owns)
- `pnpm --filter @multica/web test` passes
- Browser tab + landing pages visually verified by user (Task 3)
- DNS readiness decision recorded in SUMMARY (Task 4 outcome)
- en.ts and zh.ts have equal AlgoPlan reference counts
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebrand-pass/07-02-SUMMARY.md` per template.
Track in SUMMARY:
  - Task 4 decision outcome (a / b / c) — drives whether 07.1 follow-up plan is needed
  - Twitter handle confirmation status (@algoplan_hq vs other)
  - Domain ownership status reported by user (algoplan.ai live / pending / option-b)
  - en.ts ↔ zh.ts AlgoPlan reference counts (sanity check parity)
</output>
